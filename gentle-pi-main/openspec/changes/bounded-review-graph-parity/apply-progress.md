# Apply Progress: Bounded Review Graph Parity

## Cumulative execution ledger (reconstructed)

This ledger preserves the completed-work continuity recorded before Batch 18. Detailed historical command output was consolidated here after an artifact-write recovery; the completed task checkboxes in `tasks.md` remain the authoritative task-completion evidence.

- **Units 1–2:** delivered canonical JSON/domain hashing, strict graph event schemas, exact Git common-directory resolution, qualified/fail-closed authority locking, immutable graph objects, quorum-root publication, and deterministic graph reduction. Focused canonical/schema/repository/lock/object/graph suites and full regressions passed.
- **Unit 3:** delivered graph-backed transaction mutations, identity-bound checkpoints, runtime-branded receipts, repository-scoped authoritative gates, exact typed-target validation, and zero-actor gates. Focused authority/gate/transaction/checkpoint suites and full regressions passed.
- **Unit 4:** delivered deterministic bundle export/import, staged closure validation, idempotent publication, and an authority-distinct mirror boundary. Focused transfer/mirror/controller suites and full regressions passed.
- **Unit 5:** replaced migration/activation behavior with fail-closed legacy detection and explicit destructive reset, reset-incarnation binding, Git environment fencing, and explicit inspect/reset/recover/repair surfaces. Reset, lock, authority, bundle, gate, and schema suites passed.
- **Unit 6:** removed superseded native activation-fence code and packaging without weakening graph locking. Package-manifest, reset, lock, package-file, and full suites passed.
- **Final integration:** graph, lock, bundle, mirror, reset, receipt/gate, checkpoint, controller, repository, and package matrix passed; package verification, full suite/runtime harness, cleanup scan, and whitespace checks passed. The recorded delivery boundary remains one `size:exception` single PR; no delivery action occurred.
- **Post-escalation BRGP remediation:** BRGP-003 added canonical reducer transition/input replay validation and rejects unsupported transitions; BRGP-010 makes exact completed gate retries replay canonically while changed requests deny; BRGP-011 provides conservative ESRCH-only owner-death proof and distinct `recover-lock` behavior. Focused and full strict-TDD evidence passed; the old lineage was not reopened.

### Cumulative TDD evidence

| Work | Evidence retained |
|---|---|
| Units 1–6 and BRGP remediation | Each unit completed RED → GREEN → TRIANGULATE → REFACTOR before its persisted task checkbox was checked; focused suites and full `pnpm test` runs were recorded as passing. |
| Verification-only batches | No source/test edits; RED/GREEN was not applicable. |

## Batch 18 — Focused re-verification and new-lineage readiness complete

- Completed task: **8. Focused re-verification and new-lineage readiness**.
- Persisted task update: marked the focused readiness checkbox `- [x]` in `tasks.md` only after every required command passed.
- No production or test code changed. No review lineage, gate, delivery, commit, push, PR, release, or publication command was started.
- **BRGP-003:** focused schema and bundle coverage passed: unsupported reducer transitions are rejected; deterministic bundle export/import validates and installs the complete staged predecessor closure before authority publication.
- **BRGP-010:** exact completed gate retry returns its canonical journal result; a new idempotency key with stale binding remains denied.
- **BRGP-011:** active and ambiguous lock owners remain fail-closed; only a conclusively absent (`ESRCH`) owner supports proof-bound recovery, and absent lock recovery never force-steals.
- Old lineage evidence was read directly from `.git/gentle-ai/reviews/lineages/bounded-review-graph-parity/`: `HEAD` remains revision 5 with state hash `f1805f8f4123572245e5f6f11f8197ca0a7bff9abcfe23f9146cd8aad2bd1331`; revision 5 remains `terminal` / `escalated`. Its `HEAD` and revision file SHA-256 values were unchanged during this batch.

### Batch 18 verification evidence

- `node --experimental-strip-types --test tests/review-graph-schema.test.ts tests/review-bundle.test.ts tests/review-gate.test.ts tests/review-lock.test.ts tests/review-controller.test.ts` — PASS (29/29).
- `pnpm test` — PASS (382 node tests; declared runtime harness PASS).
- `pnpm run test:harness` — PASS.
- `node scripts/verify-package-files.mjs` — PASS (49 package resources).
- `git diff --check` — PASS.
- Terminal-lineage assertion — PASS: `HEAD.revision === 5`, `revision-5.state.phase === "terminal"`, `terminal_state === "escalated"`, and `HEAD.state_hash === revision-5.state_hash`.

### TDD Cycle Evidence

| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 8 focused re-verification | Existing BRGP-003/010/011 suites | Unit/integration | Focused 29/29 PASS | N/A — verification-only task; no code changed | N/A | Existing multi-case focused matrix PASS | N/A |

### Remaining tasks

- None. All 11 persisted implementation/readiness tasks are visibly checked.

### Workload / PR boundary

- The recorded single-PR `size:exception` remains in force. This task only establishes readiness for a **new** review lineage; the parent owns explicit `review/start` and no lineage was created here.

### Structured status consumed

- `artifactStore`: `openspec`; `applyState`: `ready`; `nextRecommended`: `apply`.
- `actionContext`: repo-local `/home/gentleman/work/gentle-pi`; allowed edit root `/home/gentleman/work/gentle-pi`.
- Strict TDD active; because this was verification-only with no source/test change, no RED → GREEN production cycle was applicable.

## Controller-authorized remediation — `bounded-review-graph-parity-r2`, revision 2

- **Authorization consumed:** frozen-ledger hash `23aa9e122ec8d0d2016c132563de383098e57e5d71922da30e98636517b3cdf2`; one authorized bounded fix batch, no additional scope, review, delivery, commit, or lineage action.
- **Fixed IDs:** `BRGP2-001`, `BRGP2-002`, `BRGP2-003`, `BRGP2-004`, and `BRGP2-005`. `BRGP2-006` was not changed (informational only).
- **Files changed for this batch:** `lib/review-repository.ts`, `lib/review-lock.ts`, `lib/review-reset.ts`, `lib/review-graph-schema.ts`, `lib/review-transaction.ts`, `tests/review-lock.test.ts`, and `tests/review-reset.test.ts`.
- **BRGP2-001:** managed review-store resolution now rejects an escaped or symlink/reparse managed path beneath the canonical Git common directory before reset can mutate it. Reset uses the resolved common-directory authority boundary.
- **BRGP2-002:** lock acquisition now persists a token-bound owner intent before publishing the lock directory. An incomplete lock may be quarantined only through proof-bound recovery of that exact dead intent; live or ambiguous ownership remains fail-closed.
- **BRGP2-003:** reset persists the generated epoch and authority-incarnation identity in durable reset state before `STORE` initialization/pointer publication. Resume reuses exactly those identities and rejects a mismatched descriptor.
- **BRGP2-004:** `gate-evaluated` is now an explicitly schema-bound `gate` replay transition. Gate events cannot be represented by another event kind.
- **BRGP2-005:** authoritative `readGraphState` now performs complete semantic replay through `validateReviewGraphReplayV1`, including reducer transition/input, request journal, gate replay, claims, event state hash, and root binding checks.

### TDD Cycle Evidence

| IDs | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|
| BRGP2-001..005 | `node --experimental-strip-types --test tests/review-lock.test.ts tests/review-reset.test.ts` failed before implementation: incomplete lock recovery API unavailable and reset state had no pre-publication incarnation. | Same focused command passed 11/11 after lock intent and durable reset identity implementation. | `node --experimental-strip-types --test tests/review-lock.test.ts tests/review-reset.test.ts tests/review-graph-schema.test.ts tests/review-gate.test.ts tests/review-bundle.test.ts` passed 32/32; `pnpm test` passed 385/385; runtime harness passed. | Centralized managed-path rejection in repository authority, explicit gate replay/schema checks, and exact resume identity reuse; no design deviation. |

### Verification evidence

- `pnpm test` — PASS (385 tests and declared runtime harness).
- `pnpm run test:harness` — PASS.
- `node scripts/verify-package-files.mjs` — PASS (49 package resources).
- `git diff --check` — PASS.

### Task and delivery state

- No task checkbox was changed: `tasks.md` already has every implementation/readiness item visibly checked, and this controller-authorized remediation is tracked by its frozen ledger rather than a new SDD task.
- Remaining unchecked task lines: none.
- Delivery boundary remains the recorded single-PR `size:exception`; this batch created no commit, PR, review, lifecycle-gate, or delivery action.
- Structured status consumed: authoritative OpenSpec status (`repo-local` workspace `/home/gentleman/work/gentle-pi`, allowed edit root identical). Its `applyState: all_done` was superseded only for this authorized remediation by `r2-remediation-authorization.md`; strict TDD remained active.

## Batch 19 — Section 9 BRGP2-001 TOCTOU remediation complete

- Completed task: **9. Terminal R2 escalation remediation — BRGP2-001 TOCTOU remediation** (checkbox marked only after focused and full-suite evidence passed).
- **Baseline before any change:** `pnpm test` — PASS (385 node tests, 0 fail; runtime harness PASS).
- **Implementation:** `lib/review-reset.ts` now performs every destructive quarantine/delete mutation through descriptor-anchored operations: the complete parent-directory chain from the canonical Git common directory is opened with `O_RDONLY|O_DIRECTORY|O_NOFOLLOW` handles, handle/path identity (bigint device and inode) is proven immediately before and after each `renameSync`/`rmSync`, and the mutation target itself is proven non-symlink after validation. A symlink, reparse point, or substituted directory discovered after validation throws `REVIEW_RESET_RACE_UNSAFE` and the reset stays fail-closed; path prechecks alone never authorize a destructive operation. A test-only `raceWindowHook` injection point fires between validation and the destructive syscall for deterministic race injection.
- **Honest platform boundary:** Node.js exposes no `renameat`/`unlinkat`; the anchored implementation is the equivalent race-safe form the task allows (O_NOFOLLOW opens plus held-handle identity proofs immediately around the syscall, fail-closed on any post-validation divergence), not a claim of kernel-atomic `*at` semantics.

### TDD Cycle Evidence

| Task | Test file | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 9 / BRGP2-001 | `tests/review-reset.test.ts` | Added 4 race-injection/symlink-replacement tests (`quarantine move rejects a legacy root replaced by a symlink inside the race window`, `quarantine move rejects a destination parent substituted by a symlink inside the race window`, `quarantine delete rejects a quarantine parent substituted by a symlink inside the race window`, `race-rejected reset stays fail-closed and completes only through explicit forward resume`); all 4 failed before implementation (`Missing expected exception ... /REVIEW_RESET_RACE_UNSAFE/`, hook API absent). | Anchored-handle implementation in `lib/review-reset.ts`; focused suite passed 10/10. | Source-swap, destination-parent-swap, and delete-parent-swap injections plus outside-sentinel integrity, fail-closed store blocking, and explicit forward resume; focused regression `tests/review-reset.test.ts tests/review-lock.test.ts tests/review-repository.test.ts tests/review-transaction.test.ts tests/review-controller.test.ts tests/review-gate.test.ts tests/review-bundle.test.ts` passed 48/48. | Centralized `openAnchoredParentChainV1`/`assertAnchoredHandleV1`/`anchoredDestructiveOperationV1` helpers; single `raceUnsafeError` vocabulary; no change to confirmation protocol, phases, or lock recovery. |

### Verification evidence

- `node --experimental-strip-types --test tests/review-reset.test.ts` — PASS (10/10, includes existing crash-injection `faultAfterPhase` resume tests).
- Focused regression (reset, lock, repository, transaction, controller, gate, bundle) — PASS (48/48).
- Full `pnpm test` after all Batch 19–21 work — PASS (398/398 + runtime harness).

### Rollback boundary

- Before destructive mutation the remediation is inert: reverting `tests/review-reset.test.ts` additions and the anchored helpers restores the previous behavior without mutating legacy state. Once mutation support is active, only the unsafe non-anchored path may be disabled; fail-closed behavior, R2 revision 5 terminality, and path-precheck-only deletion prohibition are preserved.

## Batch 20 — Section 10 R3 readiness verification complete

- Completed task: **10. R3 readiness verification** (checkbox marked only after every verification passed).
- **BRGP2-001 evidence verified:** race-injection (3 injection geometries), symlink/reparse replacement, and crash/recovery (`faultAfterPhase` at `quarantining`, `initializing`, `verifying` plus race-rejection forward resume) all pass in `tests/review-reset.test.ts`; supported-platform evidence is Linux (POSIX `O_NOFOLLOW`) with the documented Windows degradation to identity-proof-only (`O_NOFOLLOW ?? 0`) — no Windows run was performed in this batch and none is claimed.
- **Full suite:** `pnpm test` — PASS (398/398 node tests; runtime harness PASS); `node scripts/verify-package-files.mjs` — PASS (49 resources); `git diff --check` — PASS.
- **R2 revision 5 terminal escalated and immutable:** read directly from `.git/gentle-ai/reviews/lineages/bounded-review-graph-parity-r2/`: `HEAD` = `{"revision":5,"state_hash":"cd47cb077945e90fc343fac96fd70d3daa368f8d86e47db56af567361f103b4a"}`; `revisions/5.json` has `state.phase = "terminal"`, `state.terminal_state = "escalated"`, and its `state_hash` matches `HEAD`. File mtimes (19:11/19:34 local) predate all Batch 19–21 work; no write in this session touched the repository review store (all tests use `mkdtempSync` repositories). The original lineage `bounded-review-graph-parity` revision 5 also remains terminal escalated with unchanged `state_hash f1805f8f…`.
- **New lineage not started:** `.git/gentle-ai/reviews/lineages/` contains exactly `bounded-review-graph-parity` and `bounded-review-graph-parity-r2`; no review, gate, delivery, commit, push, PR, release, or publication command was run.

## Batch 21 — Section 11 release-from-main fast path parity (gentle-ai 2b3a091)

- Completed task: **11. Release-from-main fast path parity (gentle-ai 2b3a091)** — RED, GREEN, TRIANGULATE, and REFACTOR checkboxes marked after evidence passed.
- **Historical implementation record retired:** the release fast-path evaluator, command authorization, publication-target derivation, and delivery-bound wording from this batch have been removed. They are not current acceptance evidence.
- **Current reconciliation:** review outcomes and `validate` are informational. Commit, push, pull-request, and release delivery follow ordinary repository policy without Pi authorization or review-state consumption.

### TDD Cycle Evidence

The historical RED/GREEN table for the retired delivery implementation is omitted from current acceptance evidence. Current tests cover the review-only delivery boundary instead.

### Verification evidence

- `node --experimental-strip-types --test tests/review-gate.test.ts` — PASS (19/19).
- `node --experimental-strip-types --test tests/review-controller.test.ts` — PASS (6/6).
- `node --experimental-strip-types --test tests/orchestrator-budget.test.ts tests/review-ledger-contract.test.ts` — PASS (51/51).
- Final full suite: `pnpm test` — PASS (398 tests, 0 fail; runtime harness PASS); `node scripts/verify-package-files.mjs` — PASS (49 resources); `git diff --check` — PASS.

### Task and delivery state

- Checked boxes this batch set: Section 9 BRGP2-001, Section 10 R3 readiness, Section 11 RED/GREEN/TRIANGULATE/REFACTOR.
- Delivery boundary unchanged: recorded single-PR `size:exception`; no commit, push, PR, release, lifecycle-gate consumption against the real repository, or new review lineage occurred.

## Post-apply 4R correction transaction — units 1–3 of 6 (frozen ledger: `reviews/post-apply-4r-ledger.md`)

Single correction transaction fixing exactly the 6 corroborated findings. No commit created. Reference: `/home/gentleman/work/gentle-ai` (read-only) — `internal/reviewtransaction/store.go` (identity model, used for Unit 4) and `internal/reviewtransaction/gate.go` / `internal/catalog/release_event_test.go` (checked for a remote/CI-derivation model; gentle-ai has no concrete Go implementation of remote-name binding or gh-derived CI for this fast path — only prose in `internal/components/sdd/triggerrules.go` — so Units 1–2 are pi-native implementations of the documented contract).

### UNIT 1 — RISK-001 (BLOCKER) — fixed

- **RED**: `tests/review-gate.test.ts` — "release fast path denies a remote endpoint that is not the repository's actually configured remote name" (file:// URL, attacker filesystem path, non-configured remote name all asserted ineligible; configured `origin` asserted eligible). Pre-fix failure: `AssertionError: a file:// URL supplied as remote must not be eligible (true !== false)`.
- **GREEN**: same test passes after the fix; full `review-gate.test.ts` 20/20 → 21/21 including this new test.
- **Fix**: `lib/review-transaction.ts` — `resolveRemoteGateRef` no longer accepts a caller-supplied remote string directly. New `resolveConfiguredRemoteUrl(cwd, remote)` requires `remote` to match a bare-name pattern (`CONFIGURED_REMOTE_NAME`, no `/`, `:`, or `..`), then requires it to appear in `git -C <repo> remote` output (sanitized env), then resolves its URL via `git -C <repo> remote get-url <name>` (sanitized env); `ls-remote` now runs against that resolved URL, never the caller string. `lib/review-repository.ts` — exported the previously-private `reviewGitEnvironment()` so `review-transaction.ts` reuses the same sanitized-env implementation (no duplicate).
- **Files touched / line deltas**: `lib/review-repository.ts` (+1/-1, export keyword only); `lib/review-transaction.ts` (~+45/-16, `resolveRemoteGateRef` and two new helpers); `tests/review-gate.test.ts` (+30, one new test).
- **Rollback boundary**: revert `resolveRemoteGateRef`/new helpers in `lib/review-transaction.ts` to the pre-unit version (direct `spawnSync("git", ["ls-remote", "--refs", remote, ref], ...)` with the old inline string validation), drop the `export` on `reviewGitEnvironment` in `lib/review-repository.ts`, and remove the added test.

### UNIT 2 — RISK-002 (BLOCKER) — fixed

- **RED**: `tests/review-gate.test.ts` — "release fast path requires independently derived CI success via the gh CLI bound to the exact remote SHA — caller-supplied CI evidence alone is never sufficient" (self-report without gh derivation denied; `gh` erroring denied; derived success bound to the wrong SHA denied; derived success bound to the exact remote SHA eligible). Pre-fix failure: self-reporting case returned `eligible: true` (assertion expected `false`).
- **GREEN**: same test passes; also updated 3 pre-existing eligible-path tests ("proves the immutable origin/main SHA…", "fails closed on escalating or invalidating…", and the new Unit 1 "configured" sub-case) to inject a `successfulGhCommandRunner` stub, since they now require independent CI derivation to reach eligibility. `review-gate.test.ts` 22/22.
- **Fix**: `lib/review-transaction.ts` — new exported `GhCommandRunnerV1` type, `defaultGhCommandRunner` (execFile-array-form `spawnSync("gh", args, {env: sanitized})`), `deriveReleaseCiStatusForShaV1` (calls `gh api repos/{owner}/{repo}/commits/<sha>/status --jq .state`; any error/non-zero/empty output ⇒ `proven:false`), and a test-injection seam `setReleaseGhCommandRunnerForTestingV1` mirroring the existing `setReviewMutationLockPlatformForTesting` pattern. `evaluateReleaseFastPathV1` now requires both the pre-existing caller-supplied `evidence.ci` cross-check AND independent derivation to equal `"success"` for the exact resolved remote SHA; failing either fails closed.
- **Files touched / line deltas**: `lib/review-transaction.ts` (~+55, new types/functions + call site); `tests/review-gate.test.ts` (+45 net, 1 new test + stub helper + 3 call-site updates); `tests/review-controller.test.ts` (+4, global `setReleaseGhCommandRunnerForTestingV1` stub — required because the production controller path has no per-call test override, matching the file's existing `setReviewMutationLockPlatformForTesting` module-level convention).
- **Rollback boundary**: revert the `evidence.ci` block in `evaluateReleaseFastPathV1` to the single pre-existing comparison, delete `GhCommandRunnerV1`/`defaultGhCommandRunner`/`deriveReleaseCiStatusForShaV1`/`setReleaseGhCommandRunnerForTestingV1`, remove the `ghCommandRunner` stub wiring from the 4 touched test call sites and the `setReleaseGhCommandRunnerForTestingV1` line in `review-controller.test.ts`.

### UNIT 3 — RELY-001 (CRITICAL) — fixed

- **RED**: `tests/review-gate.test.ts` — "release fast path treats vX.0.0 and pre-1.0 v0.Y.0 minor bumps as major-equivalent, while v0.Y.Z patch releases remain eligible" (`v1.0.0` denied, `v0.16.0` denied, `v0.15.1` eligible). Pre-fix failure: `v0.16.0` case — `AssertionError: true !== false` (was eligible, should be denied).
- **GREEN**: same test passes; `review-gate.test.ts` 22/22.
- **Fix**: `lib/review-transaction.ts` — major-equivalence condition changed from `semver[2] === "0" && semver[3] === "0"` (minor∧patch zero, independent of major) to `semver[3] === "0" && (semver[2] === "0" || semver[1] === "0")` — denies fast path for any `vX.0.0` (major≥0) or any pre-1.0 `v0.Y.0` regardless of `Y`; patch releases `v0.Y.Z` with `Z>0` remain eligible.
- **Files touched / line deltas**: `lib/review-transaction.ts` (1 condition line changed + 3 comment lines added); `tests/review-gate.test.ts` (+33, one new test).
- **Rollback boundary**: revert the single `if` condition to `semver[2] === "0" && semver[3] === "0"` and remove the added test.

### Verification evidence (units 1–3)

- `node --experimental-strip-types --test tests/review-gate.test.ts` — PASS (22/22).
- `pnpm test` — PASS (401/401; runtime harness PASS).
- No production or test file outside `lib/review-repository.ts`, `lib/review-transaction.ts`, `tests/review-gate.test.ts`, `tests/review-controller.test.ts` was touched for units 1–3.

### UNIT 4 — RESL-001 (BLOCKER) — STOPPED, finding left open

Implemented exactly as instructed (`resolveRepositoryAuthorityV1` in `lib/review-repository.ts` re-keyed `repository_identity`/`repository_id`/`authority_id` to the canonical Git common-directory realpath instead of `git rev-list --max-parents=0 --all`), verified GREEN against a new RED test ("repository authority identity does not change when an unrelated orphan root commit appears", `tests/review-repository.test.ts`) and against updated versions of the two pre-existing tests that depended on root-commit-set identity (`tests/review-repository.test.ts` "non-Git directories and empty repositories fail closed" / `tests/review-transaction.test.ts` "repository authority fails closed until Git has stable root commit anchors").

**Discovered while running the full suite (not scoped to the 3 cited locations):** the fix breaks a separately and formally specified requirement — `openspec/changes/bounded-review-graph-parity/specs/review-graph/spec.md:151-165`, "Requirement: Validated atomic bundle transfer" / "Scenario: Valid export and import — … imported into a compatible repository" and "Scenario: Invalid bundle has no effect — … authority-forging … is rejected." `lib/review-bundle.ts:187` gates bundle import on exact `repository_identity`/`repository_id`/`authority_id` equality between the exporting and importing repository. Under root-commit-based identity, two independent clones of the same history share the same `repository_id` (portable) while each still has its own separate `store_root` (location-bound) — so a bundle exported from one clone imports cleanly into a genuinely different, empty clone (the documented "clean-clone/workstation recovery" path), while a truly unrelated repository is still rejected (`repository_id` differs). Common-directory-realpath identity is, by construction, never shared by two different directories, so after the fix **every** cross-clone import is rejected as "authority does not match" — confirmed by running `tests/review-bundle.test.ts` in isolation: 4 of 5 tests failed (`bundle export is deterministic and compatible import…`, `…reduced state does not match…`, `…claimed incarnation…`, `…malformed live STORE…`), all with `ReviewBundleError: Bundle repository authority does not match this repository` instead of their intended failure/success modes.

**Why this cannot be soundly resolved inside this correction transaction:**
- A test-only fixture change (using a linked Git worktree instead of `git clone` for the "compatible repository" fixture, so `target` shares `source`'s common directory) was tried and reverted: worktrees share not only `repository_id`/`authority_id` but also the literal `store_root` directory, collapsing "a distinct, initially-empty, compatible repository" into "the exact same store," which breaks the tests' load-bearing precondition that the target starts without the lineage before import (2 of 5 tests then failed for a new reason: `imported` was `false`/no exception was thrown, because there was nothing left to import — the state was already there).
- Preserving both properties (RESL-001's stability requirement, and the bundle spec's cross-clone-compatible-but-independently-empty requirement) needs a portable, content-derived compatibility signal that is deliberately *not* the store's own location — i.e., a second identity concept, decoupled from `resolveRepositoryAuthorityV1`'s location-bound authority, used only by `lib/review-bundle.ts`'s cross-repository check. Designing and introducing that second concept touches a file (`lib/review-bundle.ts`) outside RESL-001's cited locations (`lib/review-repository.ts:100-116`, `lib/review-object-store.ts:105-111`, `lib/review-transaction.ts:748-750`) and is a genuine new architecture decision, not a mechanical consequence of the cited fix.
- Weakening `lib/review-bundle.ts`'s authority check instead (e.g., dropping the equality requirement) would silently accept bundles from unrelated repositories into a fresh target, defeating the "authority-forging … is rejected" scenario — an unacceptable safety regression, not an acceptable substitute.

**Action taken:** reverted all Unit 4 production and test changes (`lib/review-repository.ts`, `tests/review-repository.test.ts`, `tests/review-transaction.test.ts`, and the trial `tests/review-bundle.test.ts` worktree fixture) back to their pre-correction-transaction state. `pnpm test` — PASS (401/401; unchanged from post-unit-3 baseline) confirms zero regression from the revert. RISK-001, RISK-002, and RELY-001 remain fixed; **RESL-001 remains open** pending an explicit maintainer decision on how to reconcile stable local authority identity with the bundle-transfer spec's cross-clone compatibility requirement (likely: introduce a second, explicitly content-derived "bundle compatibility" identity separate from the location-derived local store authority, scoped as its own follow-up change).

### UNIT 5 — RESL-002 (BLOCKER) — fixed

- **RED**: `tests/review-lock.test.ts` — "stale lock recovery succeeds via the default production platform without an injected test adapter" (constructs `ReviewMutationLockV1` with no platform argument, simulates a crashed-owner `owner.json` with a provably-dead PID, calls `lock.recover(...)`). Pre-fix failure: `ReviewLockError: Review authority lock recovery race; re-observe: Review lock recovery is unsupported without a qualified no-replace move primitive` — i.e., the exact bug: production recovery always throws.
- **GREEN**: recovery now succeeds; lock directory removed, quarantine directory created at the deterministic `stale-<hash>-<token>` path. Second new test, "default production lock platform preserves no-replace semantics when the quarantine destination is already occupied" (pre-creates the deterministic destination with sentinel content before recovering), asserts `recover()` still throws `ReviewLockError` and the pre-existing destination content is untouched — this test incidentally already passed pre-fix (the old code always threw for the wrong reason: "unsupported", not "already occupied"); post-fix it throws for the correct reason (`mkdirSync` EEXIST), confirmed by inspecting the thrown message. `review-lock.test.ts` 8/8.
- **Fix**: `lib/review-lock.ts` — new exported `qualifiedNodeFsLockPlatformV1()`: a pure-Node atomic no-replace `moveNoReplace`. The lock's moved paths are directories (not files) — `fs.linkSync` cannot hard-link directories on POSIX — so for a directory source it uses `fs.mkdirSync(destination)` (itself an atomic no-replace primitive: EEXIST whether the destination is a file or directory, empty or not) to exclusively reserve the destination name, then `fs.renameSync(source, destination)` to fold the source's content into that just-reserved, still-empty destination (a rename onto one's own fresh empty directory is an ordinary, safe POSIX replace); for a plain file source it uses the classic `linkSync` + `unlinkSync` sequence. Any failure is wrapped in a descriptive `ReviewLockError` (never the old unconditional "unsupported" throw). `ReviewMutationLockV1`'s constructor now defaults to this qualified platform (`DEFAULT_PLATFORM`) instead of `UNQUALIFIED_PLATFORM`; `UNQUALIFIED_PLATFORM` itself is unchanged and still available for explicit injection (e.g., simulating a truly unqualified environment), but is no longer the default anywhere. `extensions/gentle-ai.ts:2930,2937` (`INSPECT`/`RECOVER_LOCK` operations) construct `ReviewMutationLockV1` with no platform argument, so they automatically pick up the new qualified default — no edits needed there. `ReviewTransactionStore`'s existing `options.mutationLockPlatform ?? mutationLockPlatformForTesting` wiring also resolves to the new default when neither is supplied.
- **Files touched / line deltas**: `lib/review-lock.ts` (+54, new function + import additions + one default-value change); `tests/review-lock.test.ts` (+34, one helper + 2 new tests); `extensions/gentle-ai.ts` unchanged (benefits automatically from the class-level default).
- **Rollback boundary**: delete `qualifiedNodeFsLockPlatformV1`/`DEFAULT_PLATFORM` from `lib/review-lock.ts`, restore the constructor's default parameter to `UNQUALIFIED_PLATFORM`, revert the added `node:fs` imports, and remove the 2 added tests + `writeStaleOwner` helper from `tests/review-lock.test.ts`.

### UNIT 6 — RESL-003 (CRITICAL) — fixed

- **RED**: three new tests in `tests/review-object-store.test.ts`:
  - "genesis quorum-loss crash is forward-recoverable when exactly one unambiguous candidate root set exists" — installs a genesis root set, uses the existing `faultInjector` hook to throw before `CURRENT.1` is written (simulating a crash after 1-of-3 slots), then calls `repairCurrentPointers()`. Pre-fix failure: `ReviewObjectStoreError: CURRENT pointer has no valid quorum` thrown by `repairCurrentPointers()` itself (no fallback).
  - "genesis repair stays fail closed when two candidate root sets are ambiguous" and "…when the sole candidate root set is corrupted" — these incidentally already passed pre-fix (the old code fails closed for every quorum loss, for the wrong general reason); they remain meaningful post-fix because they now exercise the new ambiguity/corruption checks specifically.
  - `tests/review-reset.test.ts` — "reset resume forward-recovers a genesis quorum-loss crash in the graph-v1 store instead of throwing uncaught": completes a destructive reset, deletes `CURRENT.1`/`CURRENT.2` from the resulting graph-v1 store to simulate a crash, then calls `destructiveResetReviewAuthorityV1({ resume: true, ... })` again. Pre-fix failure: uncaught `ReviewObjectStoreError: CURRENT pointer has no valid quorum` from `lib/review-reset.ts:161`.
  - `tests/review-bundle.test.ts` — "bundle import forward-recovers a genesis quorum-loss crash in the target store instead of silently discarding its existing lineage": imports a bundle establishing `bundle-lineage` in a target store, deletes `CURRENT.1`/`CURRENT.2` to simulate a crash, then imports a second bundle adding `second-lineage`. Pre-fix failure: `ReviewIntegrityError: Graph lineage is missing from authoritative root set` when reading back `bundle-lineage` — the old blanket `catch {}` around `readCurrent()` misclassified the recoverable crash as an empty store and published a competing generation-0 root set that silently discarded the pre-crash lineage (a real, latent data-loss bug beyond what the ledger described).
- **GREEN**: all 3 new object-store tests pass; `review-object-store.test.ts` 8/8. `review-reset.test.ts` 11/11. `review-bundle.test.ts` 6/6.
- **Fix**:
  - `lib/review-object-store.ts` — `repairCurrentPointers()` now catches a `readQuorum()` failure and calls new private `discoverSoleGenesisRootSet()`: scans every object file under `roots/sha256/**` (not just surviving pointers); if and only if **exactly one** object exists in the entire store, it hash-verifies via the existing `readRootSet()` identity check, and its body is `generation === 0` with `predecessor_root_set_id === null` matching this store's `repository_id`/`authority_id`, it is accepted as the sole unambiguous genesis candidate and all 3 `CURRENT` slots are reconstructed from it (`writePointer(0|1|2, candidate, 0|1|2)`, mirroring `publishRootSet`'s own generation-0 slot-epoch numbering). Any other outcome (zero objects, more than one object — including an orphaned non-genesis object from a separately-failed publish attempt, corrupted/hash-mismatched sole object, or a sole object that isn't a genesis candidate) re-throws the original `readQuorum()` error unchanged, preserving the existing pre-unit test "root successors must name the current root and repair restores only a quorum-backed pointer" (which relies on an orphaned generation-1 object making repair ambiguous and failing closed).
  - `lib/review-reset.ts` — new private `readCurrentWithGenesisRepair(graph)`: tries `graph.readCurrent()`, and on failure calls `graph.repairCurrentPointers()` then retries once; if repair itself throws (genuinely ambiguous), the original `readCurrent()` error propagates unchanged. Both call sites at (former) lines 160–161 now route through this helper instead of calling `graph.readCurrent()` directly, so reset-resume forward-recovers a genesis crash instead of throwing uncaught.
  - `lib/review-bundle.ts` — `ReviewBundleImporter.import()`'s `let current; try { current = this.#store.readCurrent(); } catch {}` now attempts `repairCurrentPointers()` + one retry inside the catch before falling back to `current = undefined`, so a recoverable genesis crash is repaired rather than misclassified as an empty store (which previously would have silently published a competing, data-losing generation-0 root set).
- **Files touched / line deltas**: `lib/review-object-store.ts` (+1 import, +~35 lines: repair fallback branch + `discoverSoleGenesisRootSet`); `lib/review-reset.ts` (+14, helper + 2 call-site edits); `lib/review-bundle.ts` (+8, catch-block repair-and-retry); `tests/review-object-store.test.ts` (+38, 3 new tests); `tests/review-reset.test.ts` (+21, 1 new test); `tests/review-bundle.test.ts` (+27 net: `unlinkSync` import, `createLineage` gained an optional second `lineageId` parameter defaulting to `"bundle-lineage"` — backward compatible with every existing call — plus 1 new test).
- **Rollback boundary**: revert `repairCurrentPointers()`/remove `discoverSoleGenesisRootSet()` in `lib/review-object-store.ts`; remove `readCurrentWithGenesisRepair` and restore the two direct `graph.readCurrent()` call sites in `lib/review-reset.ts`; restore the bare `try { current = this.#store.readCurrent(); } catch {}` in `lib/review-bundle.ts`; remove the added tests in the 3 test files and revert `createLineage`'s signature to a single `cwd` parameter.

### Verification evidence (units 1–3, 5–6)

- `pnpm test` — PASS (408/408; runtime harness PASS).
- `git diff --check` — PASS (no whitespace errors).
- RESL-001 (Unit 4) is the sole open finding; RISK-001, RISK-002, RELY-001, RESL-002, and RESL-003 are fixed and verified.

## Maintainer decision: pinned root-set subset identity (resolves RESL-001)

The maintainer made an explicit architecture decision to resolve the previously-escalated RESL-001 (repository/authority identity recomputed on every call from the live root-commit set, permanently orphaning the store when an unrelated orphan branch or subtree merge is added): **pin the root-commit set once at store initialization and validate `pinned ⊆ live` on every later access, instead of recomputing identity.** This supersedes the Unit-4 revert recorded above (which reverted an earlier common-directory-identity attempt because it broke bundle cross-clone portability); the pinned-subset model keeps identity stable under orphan-branch/subtree-merge growth while still deriving identity from repository content (not location), so bundle portability is unaffected.

- **RED**: added 4 new tests to `tests/review-repository.test.ts` (2 pre-existing tests kept unchanged):
  - "repository identity is pinned once and reused on later resolutions" — two consecutive `resolveRepositoryAuthorityV1(root)` calls on the same repo return identical `repository_id`/`authority_id`/`root_commit_ids` (baseline pin-and-reuse behavior).
  - "an orphan root added after store establishment leaves identity unchanged and reads/mutations still succeed" — establishes a lineage (pins identity from the repo's single root commit), adds a brand-new orphan-branch root commit (`git checkout --orphan gh-pages` + commit), then re-resolves authority and performs both a read (`ReviewTransactionStore.forRepository(root).read(...)`) and a mutation (`createLineage(root, "pin-lineage-2")`). Pre-fix failure: `repository_id`/`authority_id` changed after the orphan commit was added (assertion `actual !== expected` on the hash comparison) — this is the exact RESL-001 bug, reproduced live.
  - "a store transplanted into an unrelated repository fails closed" — establishes a store in a source repo, copies the entire `<common-dir>/gentle-ai/reviews` directory into a freshly-initialized, unrelated target repo (disjoint root commit), and asserts `resolveRepositoryAuthorityV1(target)` throws `/pinned|authority/i`. Pre-fix failure: "Missing expected exception" (no subset validation existed; the old code would have silently recomputed target's own live identity and treated the transplanted store as compatible-or-broken by unrelated means, never failing closed on the pin itself).
  - "removing a pinned root commit via history rewrite violates the subset and fails closed" — a repo with two independent root commits (main + a second orphan root) is resolved once (pinning both roots), then the branch anchoring the second root is deleted (`git branch -D`), shrinking the live root-commit set below the pinned set; asserts the next `resolveRepositoryAuthorityV1(root)` call throws `/pinned|authority/i`. Pre-fix failure: "Missing expected exception" (no subset check existed).
  - Existing "non-Git directories and empty repositories fail closed" test (empty-repo initialization behavior) required no changes and stayed green throughout — confirming empty-repository behavior is unchanged.
- **GREEN**: implemented pinned-identity persistence and subset validation entirely inside `lib/review-repository.ts`'s `resolveRepositoryAuthorityV1`, the single function every call site (`lib/review-object-store.ts`, `lib/review-transaction.ts`, `lib/review-bundle.ts`, `lib/review-reset.ts`) already funnels through — none of those 4 files needed any changes, since they only consume the `repository_id`/`authority_id`/`repository_identity`/`store_root` fields this function returns:
  - New `IDENTITY` file persisted at `store_root` (i.e. `<git-common-dir>/gentle-ai/reviews/IDENTITY`, a sibling of `graph-v1/`, independent of the optional destructive-reset `STORE`/epoch descriptor so ordinary first-time stores — which never call `initializeDestructiveReset` — still get a pin).
  - `resolveRepositoryAuthorityV1` now: resolves `common_directory` and `store_root` (both location-derived, unaffected by identity); always recomputes the **live** root-commit set via `git rev-list --max-parents=0 --all` (unchanged validity checks: non-empty, deduplicated, valid object IDs — preserving existing empty-repository behavior verbatim); then reads any persisted pin at `store_root/IDENTITY`.
    - If a pin exists: verifies `pinned.root_commit_ids ⊆ live` (and matching `object_format`); on subset violation throws `ReviewRepositoryError("Repository root commit authority no longer matches the pinned store identity")` — fail-closed, same class/semantics as the pre-existing authority-mismatch errors. `repository_id`/`authority_id` are derived from the **pinned** identity, never the live set.
    - If no pin exists (first-ever resolution for this repository): the current live set becomes the pinned set and is durably persisted (`writeFileSync` with `wx` + fsync file + fsync directory, mirroring the existing `STORE`-file write pattern; an `EEXIST` race from a concurrent first-writer is resolved by reading back whatever was persisted first, first-writer-wins, no error).
  - A history rewrite that removes a pinned root commit is intentionally NOT recoverable — it fails closed via the same subset check (documented in the new code comment block directly above `resolveRepositoryAuthorityV1`).
  - `pnpm test` — PASS (412/412 — the pre-existing 408 plus the 4 new tests above; runtime harness PASS).
  - `git diff --check` — PASS (clean).
- **Files touched / line deltas**: `lib/review-repository.ts` (122 → 210 lines, +88: new `node:fs` imports (`mkdirSync`, `openSync`, `closeSync`, `readFileSync`, `writeFileSync`, `fsyncSync`) and `canonicalJsonV1`/`parseCanonicalJsonV1` from `review-canonical.ts`; new `IDENTITY_FILENAME`, `pinnedIdentityPathV1`, `isValidRepositoryIdentityBodyV1`, `readPinnedRepositoryIdentityV1`, `writePinnedRepositoryIdentityV1`; `resolveRepositoryAuthorityV1` rewritten to pin-and-validate-subset instead of recompute-every-call). `tests/review-repository.test.ts` (44 → 130 lines, +86: `cpSync`/`dirname` imports, `ReviewTransactionStore`/`createReviewState`/`REVIEW_MODE`/`setReviewMutationLockPlatformForTesting`/`REVIEW_LENS`/`REVIEW_ROUTE`/fixture imports, `createLineage` + `addOrphanRoot` helpers, and the 4 new tests described above). No changes to `lib/review-object-store.ts`, `lib/review-transaction.ts`, `lib/review-bundle.ts`, or `lib/review-reset.ts` — confirmed by re-running the full suite, including all 6 bundle-portability tests (`tests/review-bundle.test.ts`, unchanged, 6/6 green) and the object-store/reset/transaction suites, with zero edits to those files.
- **Rollback boundary**: revert `lib/review-repository.ts` to its pre-work-unit state (restore the single unconditional `anchors = gitLines(...)` identity derivation and drop the pin read/write helpers + `IDENTITY_FILENAME`); remove the 4 new tests plus the `createLineage`/`addOrphanRoot` helpers and their added imports from `tests/review-repository.test.ts`. No other files require reverting — the fix's entire footprint is these two files.

## Correction round 2 (graph-identity 4R)

Bounded correction transaction fixing 3 corroborated findings from a subsequent 4R pass over `lib/review-repository.ts` and interacting modules. Single correction actor, one commit-free working-tree change, three atomic work units. Baseline `pnpm test` was run first (412/412, matching the prior recorded state) and last (414/414, +2 from this round's new tests) to bound the transaction.

### Unit 3 — RESL2-002 (CRITICAL): torn read of a racing first-time IDENTITY write

- **Root cause**: `writePinnedRepositoryIdentityV1` wrote directly to the final `IDENTITY` path via `writeFileSync(path, bytes, { flag: "wx" })`. `O_CREAT|O_EXCL` makes the file's directory entry visible the instant `open()` returns, before the subsequent `write()` completes — so a second process racing the same first-time pin could observe an empty/partial file at that exact path (either via its own `EEXIST → readPinnedRepositoryIdentityV1` fallback, or via the unconditional outer read at the top of `resolveRepositoryAuthorityV1`) and die on a non-retriable `"Pinned repository identity is malformed"` throw, even though the race resolves within microseconds.
- **RED**: new test `tests/review-repository.test.ts` — "a reader racing the first-time IDENTITY write recovers once the concurrent writer completes, instead of dying on a transient partial read". Deterministically reproduces the exact race window described above (rather than relying on flaky true multi-process timing): pre-creates a **0-byte** file at the store's `IDENTITY` path via `writeFileSync(identityPath, Buffer.alloc(0), { flag: "wx" })` — simulating a concurrent writer whose `open(O_CREAT|O_EXCL)` has succeeded but whose content write has not yet landed — then calls `resolveRepositoryAuthorityV1(root)`. A new test-only hook (`setReviewRepositoryIdentityRetryHookForTesting`) fires on the first failed parse attempt and, from the hook, completes the racing writer's content (`writeFileSync(identityPath, validBody, { flag: "r+" })`), simulating the moment the real concurrent writer finishes. Pre-fix failure: `ReviewRepositoryError: Pinned repository identity is malformed` (uncaught, non-retriable — confirmed by running this exact test against the pre-fix `readPinnedRepositoryIdentityV1`, which had no retry loop and no atomic-install seam to hook into).
- **GREEN**: `tests/review-repository.test.ts` 7/7.
- **Fix** (`lib/review-repository.ts`):
  - `writePinnedRepositoryIdentityV1` now writes content to a process-unique temporary file in `store_root`, fsyncs it, then installs it via `linkSync(temporary, path)` (mirroring the write-once `installImmutable` pattern in `lib/review-object-store.ts:190-205`). The final `IDENTITY` path therefore either does not exist yet or already holds fully-written bytes — never a partial write — for any writer that goes through this function. On `EEXIST` from `linkSync` (another writer won the race), it falls back to `readPinnedRepositoryIdentityV1` exactly as before, but that read can now never observe content this function itself half-wrote.
  - `readPinnedRepositoryIdentityV1` additionally gained a small bounded retry (5 attempts, ~4ms synchronous backoff via `Atomics.wait`, ~16ms worst case) around parse failures only — never around schema-validation failures, which remain an immediate, non-retriable `"Pinned repository identity is invalid"` since those indicate genuine corruption rather than a transient race. This covers the residual case of an external/legacy actor leaving a transiently incomplete file at the path, closing the loop the task explicitly flagged as conditional ("only if the atomic-rename approach still leaves any visible-partial window").
  - Single-process behavior is unchanged: the normal (no-contention) path reads/writes once, with no added latency.
- **Files touched / line deltas**: `lib/review-repository.ts` (211 → ~267 lines at this point, +56: 2 new `node:fs` imports (`linkSync`, `unlinkSync`), retry-hook setter + bounded-retry loop in `readPinnedRepositoryIdentityV1`, atomic temp+fsync+link install in `writePinnedRepositoryIdentityV1`). `tests/review-repository.test.ts` (131 → 156 lines, +25: hook import + 1 new test).
- **Rollback boundary**: revert `readPinnedRepositoryIdentityV1`/`writePinnedRepositoryIdentityV1` in `lib/review-repository.ts` to their pre-round-2 direct-`writeFileSync`/no-retry form; remove `setReviewRepositoryIdentityRetryHookForTesting` and its module-level hook variable; remove the 1 new test and its import from `tests/review-repository.test.ts`.

### Unit 1 — RESL2-001 + RELY2-001 (BLOCKER/CRITICAL, same root cause): broken pin permanently bricks the store

- **Root cause**: `destructiveResetReviewAuthorityV1` (`lib/review-reset.ts`) called `resolveRepositoryAuthorityV1(options.cwd)` as its unconditional first line, and `inspectLegacyReviewAuthorityV1` (called 3 times inside the same function) had the identical dependency. Both are fail-closed by design for ordinary access, but that means the *only* recovery tool for a broken pin (a pinned root commit removed by ordinary `git branch -D` / history rewrite, which the pre-existing subset check correctly rejects) threw before any quarantine/reinitialize logic could run — the store was permanently bricked with no recovery path.
- **RED**: new test `tests/review-reset.test.ts` — "explicit allowBrokenIdentity recovery re-pins a store whose pinned root commit was removed by history rewrite, while ordinary access and a plain reset both stay fail-closed". Establishes a repo with 2 root commits (main + an orphan branch, mirroring `tests/review-repository.test.ts:121-130`'s pattern), pins identity by creating a lineage, then `git branch -D`s the orphan root. Asserts: (a) ordinary `resolveRepositoryAuthorityV1`/`ReviewTransactionStore.forRepository` still fail closed; (b) a **default** (non-recovery) `destructiveResetReviewAuthorityV1` call also still fails closed (no `allowBrokenIdentity`); (c) the explicit recovery call (`allowBrokenIdentity: true`, confirmation from `inspectLegacyReviewAuthorityV1(cwd, { allowBrokenIdentity: true })`) succeeds, quarantines, and re-pins from the live (now single-root) set; (d) the store is usable afterward (`readCurrentAuthority()` reads 0 lineages, and a brand-new lineage can be created and read back). Pre-fix failure (confirmed by temporarily reverting the `allowBrokenIdentity` guard-lifting logic in `lib/review-reset.ts` and re-running): `ReviewRepositoryError: Repository root commit authority no longer matches the pinned store identity`, thrown by `resolveRepositoryAuthorityV1` at the function's very first line, exactly matching the described bug. Restored the fix afterward and re-confirmed GREEN.
- **GREEN**: `tests/review-reset.test.ts` 12/12.
- **Fix**:
  - `lib/review-repository.ts`: factored the live Git probe (common directory, object format, shallow check, store root, live root-commit anchors) out of `resolveRepositoryAuthorityV1` into a shared `probeLiveRepositoryV1`. Added `resolveRepositoryAuthorityForRecoveryV1` — same probe, but never throws on a broken subset: it reports `identity_broken: true` and computes a fresh, **not-yet-persisted** identity from the current live root set instead. `resolveRepositoryAuthorityV1`'s own fail-closed behavior for ordinary access is completely unchanged (same checks, same error messages, same order). Also exported `writePinnedRepositoryIdentityV1` and `IDENTITY_FILENAME` (previously module-private) for the reset module's re-pin step.
  - `lib/review-legacy-detector.ts`: `inspectLegacyReviewAuthorityV1(cwd, options)` gained an optional `{ allowBrokenIdentity?: boolean }` parameter that selects the recovery resolver instead of the fail-closed one; `LegacyInspectionV1` gained an `identity_broken: boolean` field (always `false` when the ordinary resolver is used).
  - `lib/review-reset.ts`: `DestructiveResetOptionsV1` gained `allowBrokenIdentity?: boolean` (never inferred/defaulted — an operator must pass it explicitly). When set, the function resolves authority via the recovery path and threads the option through every `inspectLegacyReviewAuthorityV1` call. The pre-existing "requires detected legacy or mixed authority" guard now also accepts `allowBrokenIdentity && inspection.identity_broken` as a valid reason to proceed (in addition to legacy/mixed detection) — the default (`allowBrokenIdentity` unset) path is completely unaffected and still throws exactly as before. The reset state persists a new `identity_recovery: boolean` flag at creation (never re-derived on resume, for idempotent phase replay); when true, `"IDENTITY"` is added to the same generic quarantine-move/delete `roots` list already used for `lineages`/`locks`/etc. (the existing rename/rm logic already works on files, not just directories), and the `"initializing"` phase additionally calls `writePinnedRepositoryIdentityV1(authority.store_root, authority.repository_identity)` to re-pin the fresh identity before generating the new store epoch/incarnation — idempotent across crash-resume thanks to Unit 3's atomic-install fix.
- **Files touched / line deltas**: `lib/review-repository.ts` (further +~55 lines on top of Unit 3's baseline: `probeLiveRepositoryV1` extraction, `RepositoryAuthorityRecoveryV1` interface, `resolveRepositoryAuthorityForRecoveryV1`, 2 new `export` keywords). `lib/review-legacy-detector.ts` (56 → 63 lines, +7: options param, `identity_broken` field, `blocked()` signature/return update). `lib/review-reset.ts` (184 → 199 lines, +15: import additions, `allowBrokenIdentity` option + doc comment, `identity_recovery` state field, guard update, conditional `IDENTITY` root, re-pin call). `tests/review-reset.test.ts` (283 → 349 lines, +66: 4 new imports, `currentBranch`/`addOrphanRoot`/`createLineage` helpers, 1 new test).
- **Rollback boundary**: revert `lib/review-reset.ts`'s `allowBrokenIdentity` plumbing (options field, guard condition, `identity_recovery` state field, conditional `IDENTITY` root, re-pin call) back to unconditional `resolveRepositoryAuthorityV1`/`inspectLegacyReviewAuthorityV1(options.cwd)` calls and the original 6-entry `roots` array; revert `lib/review-legacy-detector.ts`'s options param and `identity_broken` field; remove `resolveRepositoryAuthorityForRecoveryV1`/`probeLiveRepositoryV1` from `lib/review-repository.ts` (folding the probe back into `resolveRepositoryAuthorityV1` directly); remove the new test and helpers from `tests/review-reset.test.ts`.

### Unit 2 — RISK2-001 (CRITICAL): STOPPED — finding left open, not faked

The prescribed fix ("add the `common_directory` equality check to `ReviewBundleImporter.import`, mirroring the transaction gate at `review-transaction.ts:1666`") cannot be implemented as specified without breaking the exact legitimate-portability guarantee the task also required to preserve. This unit was stopped; **RISK2-001 remains open**.

**Investigation performed:**

1. **Read the actual purpose of the gate's `common_directory` check** (`lib/review-transaction.ts:1532-1542` `assertCurrentRepositoryAuthority`, and `:1663-1666` `validateAuthoritativeGate`). In both cases, `common_directory` is compared between two authority resolutions **for the same `cwd`, in the same process, at two different times** (store-construction time vs. gate-validation time). Its purpose is detecting that the *live* authority for one repository has drifted since the store/receipt was created (e.g. a symlink swap, or the repo being torn down and recreated) — a same-repository staleness/tamper check. It is never used to compare two *different* physical repositories' `common_directory` values against each other.
2. **Read the actual bundle-import code path** (`lib/review-bundle.ts:184-187`): the ONLY check today is `repository_identity`/`repository_id`/`authority_id` equality between the bundle manifest and `this.#authority` — all three are hashes derived purely from the pinned root-commit set (never from `common_directory`).
3. **Read the reference model** at `/home/gentleman/work/gentle-ai/internal/reviewtransaction/bundle.go` (read-only, as instructed). Its `ImportBundle`/`validateBundleExpectation` do not carry or compare any `repository_identity`/`repository_id`/`common_directory`-equivalent field at all. Its `AuthoritativeStore` (`store.go:46-73`) derives the store path **directly from the physical Git common directory of the repo the call is running against** — there is no abstract identity blob to forge in the first place. Its actual anti-forgery binding is instead literal content equality: `expected.Snapshot.CandidateTree`, `PathsDigest`, and a re-derived `IntendedUntrackedProof` must match the **current, live candidate tree of the destination repository** (`bundle.go:222-228`). This is a fundamentally different architecture (physical-path-derived store + live-content binding) than gentle-pi's `review-bundle.ts` (abstract graph-object-store transfer keyed by a portable root-commit-hash identity) — there is no "correct bundle-bound field" in the Go reference to port over; the two designs solve the cross-repository trust problem by different means, and Go's approach doesn't need a common_directory field because it never abstracts identity away from the physical repository at all.
4. **Empirically verified `common_directory` is clone-specific**, not a shared/portable value, using the exact scenario `tests/review-bundle.test.ts`'s `repository()` helper builds (source repo + `git clone` target): `git -C source rev-parse --path-format=absolute --git-common-dir` → `/tmp/.../source/.git`; `git -C target rev-parse --path-format=absolute --git-common-dir` → `/tmp/.../target/.git` — different absolute paths — while `git rev-list --max-parents=0 --all` returns the **identical** root-commit hash in both (clones share history). This confirms: adding a literal `manifest.common_directory !== this.#authority.common_directory` equality check to `ReviewBundleImporter.import` would make the "bundle export is deterministic and compatible import installs its exact graph closure atomically" test (and every other cross-clone scenario in the 6-test `review-bundle.test.ts` suite, all of which use `source`/`target` as separate clones) fail immediately — a real regression, not a false alarm.

**Why the finding cannot be soundly closed within this unit's scope:**

- The forgery scenario is real (an attacker who has read access to a public repo's root commit object(s) — or a leaked bundle manifest revealing `repository_identity` — can graft just those commit(s) as an orphan root in an unrelated scratch repo and obtain a colliding `repository_id`/`authority_id`, since `resolveRepositoryAuthorityV1` only requires exact match of the pinned root-commit *set*, not full shared history).
- But the only concrete field this codebase uses to distinguish "the same physical repository" from "a different one" (`common_directory`) is **not stable across legitimate clones**, which is the exact scenario bundle export/import exists to support. Enforcing it would defeat the feature's own purpose and break already-passing, intentional tests.
- The reference model's actual defense (live content-tree binding) is not a small, bounded, drop-in addition to `ReviewBundleImporter.import` — `review-bundle.ts`'s bundle format has no concept of "candidate tree" or "current live review transaction" to bind against; it transfers graph objects keyed by abstract lineage/authority identity. Building an equivalent binding would require designing a new cross-repository trust primitive (e.g. bundle signing, an explicit trust list, or extending pinned identity to a broader, non-`O(root-count)` portion of the commit graph) — a genuine architectural change, not a correction-unit-sized fix, and out of scope for "fix exactly the 3 corroborated findings... no unrelated refactors."

No code was changed for this unit. RISK2-001 remains open and should be routed back for an explicit design decision (maintainer call on which trust primitive to add) rather than closed by a mechanical patch that would either break portability or provide no real security improvement.

### Final verification (correction round 2)

- Baseline check (before any change): `pnpm test` — PASS (412/412; runtime harness PASS).
- Final check (after Units 1 and 3; Unit 2 made no code changes): `pnpm test` — PASS (414/414 — the 412 baseline plus this round's 2 new tests; runtime harness PASS).
- `node --experimental-strip-types --test tests/review-repository.test.ts` — PASS (7/7).
- `node --experimental-strip-types --test tests/review-reset.test.ts` — PASS (12/12).
- Whitespace check: all 5 touched files (`lib/review-repository.ts`, `lib/review-legacy-detector.ts`, `lib/review-reset.ts`, `tests/review-repository.test.ts`, `tests/review-reset.test.ts`) are untracked in Git (part of the not-yet-committed parent feature branch), so `git diff --check` has no tracked hunks to inspect; a direct trailing-whitespace scan (`rg -n ' +$'`) over all 5 files found zero matches.
- No commit, push, PR, or release action was taken. No review lineage, gate, or delivery command was started.

## Deferral: graph-v1 cross-repo bundle trust (RISK2-001) — experimental, deferred

**Maintainer decision:** ship the parity-that-matters (release-from-main fast path, canonical hashing, lock, receipts, gates, pinned-subset identity — done and green) and defer closing `RISK2-001` to its own follow-up change, `openspec/changes/cross-repo-bundle-trust`, rather than block the release on an architectural cross-repo trust primitive.

**Why deferred, restated:** Unit 2 above already establishes, with direct investigation of `lib/review-transaction.ts`'s `common_directory` gate, `lib/review-bundle.ts`'s import path, and the `gentle-ai` Go reference model (`internal/reviewtransaction/bundle.go`, read-only), that `common_directory` cannot discriminate a legitimate same-repository clone from a forged foreign identity — it legitimately differs across clones of the exact same repository, which is precisely the scenario bundle export/import exists to support. The Go reference solves this with a structurally different, physical-path-plus-live-content-tree binding that has no equivalent field in `review-bundle.ts`'s abstract graph-object-store transfer model. Closing `RISK2-001` for real requires a genuine new cross-repo trust primitive (signed manifest, content-tree binding, or trust-on-first-use pinning), not a bounded correction — hence the separate follow-up change rather than reopening this terminal, escalated review lineage.

**Guard added so the gap is non-silent:** `ReviewBundleImporter.import` (`lib/review-bundle.ts`) now takes an experimental `acknowledgeUntrustedBundleSource?: boolean` import option (default false/absent). Adopting any lineage this store's own authority has not already established fails closed with a distinct `REVIEW_BUNDLE_UNTRUSTED_SOURCE` error unless the caller explicitly passes `acknowledgeUntrustedBundleSource: true` — an experimental, operator-attested trust decision pending the follow-up primitive. Re-importing or extending an already-locally-established lineage is unaffected and needs no acknowledgement. `extensions/gentle-ai.ts`'s `import` controller operation was wired to accept and forward this same flag (`acknowledgeUntrustedBundleSource: "true"` string parameter) so the real CLI/tool surface stays usable, not just the unit tests.

**Test evidence:** `tests/review-bundle.test.ts` — the original 6 tests were updated to pass `acknowledgeUntrustedBundleSource: true` on the import calls that adopt a lineage for the first time (re-imports of an already-known lineage are unaffected and still pass no flag); 3 new tests added: import of a brand-new lineage denied without the acknowledgement (`REVIEW_BUNDLE_UNTRUSTED_SOURCE`), import of a foreign clone's independently-fabricated lineage denied the same way despite a matching `repository_identity` (directly demonstrating the RISK2-001 vector is symmetric — a legitimate bundle and a foreign one are indistinguishable by identity alone), and import allowed once the caller explicitly acknowledges the untrusted source. Full suite: `pnpm test` — PASS (417/417; the 414 baseline plus this round's 3 new tests; runtime harness PASS).

**What ships vs. what's deferred:** same-repo/same-clone portability ships and works (with the caller passing the explicit opt-in for first-time lineage adoption); untrusted cross-repo import of a brand-new lineage requires explicit operator attestation pending the follow-up trust primitive designed in `openspec/changes/cross-repo-bundle-trust`.
