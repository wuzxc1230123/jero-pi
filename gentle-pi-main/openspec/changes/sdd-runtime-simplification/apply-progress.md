# Apply progress: sdd-runtime-simplification

## Pi unit 1 preflight — blocked

Consumed `/tmp/sdd4484-pi-unit1-status.json`: `gentle-ai.sdd-status` v2, selected change `sdd-runtime-simplification`, OpenSpec store, apply ready, no blocked reasons. Action context is repo-local with workspace and allowed root `/home/gentleman/work/gentle-pi-worktrees/sdd-runtime-simplification`. Proposal, all three specs, design, tasks, and config were read directly. No prior apply-progress existed.

Execution ownership instructions conflict: injected executor instructions require executor-side acquire/continuation and settlement, while the assignment and parent clarification prohibit those operations and reserve the existing admitted attempt to the parent. Parent confirmed prior admission but cannot change the higher-priority executor contract through a tool reply. Stopped before runtime work rather than duplicate or close the parent's attempt. No ledger operation or token persistence occurred.

## Completion and remaining work

No implementation task completed; persisted tasks remain unchanged. Tasks 2.1, 2.2, and 2.3 remain unchecked. Accepted external AI tasks are untouched; Pi units 2–4 were not started. Required next action: reconcile executor launch/settlement ownership in the injected instructions, then resume this unit under the existing parent's authority without inventing another attempt.

## TDD Cycle Evidence

| Stage | Command / assertions | Result |
|---|---|---|
| Preservation | Not run | Pre-runtime instruction conflict |
| RED | Not run | No test authored |
| GREEN | Not run | No production code edited |
| TRIANGULATE | Not run | No assertions evaluated |
| REFACTOR | Not run | No refactor |

Required focused node tests, `pnpm test`, `pnpm run check:runtime-modules`, and `git diff --check` were not run. No exit statuses, producer identity, invocation logs, acceptance, or rollback execution are claimed.

## Files, rollback, workload

Only this progress artifact was created by this executor. No runtime/source/test/agent asset changed; no runtime process was launched, so no runtime cleanup or production rollback is necessary. Removing this newly created progress artifact alone reverts this executor's filesystem contribution; preserve all pre-existing work and native history.

Assigned boundary: canonical 2.1–2.3 only, one eventual Pi PR with explicit `exception-ok` / `size-exception`, no chain. Parent forecast 230–315 authored A+D and native cap 400 were consumed, not independently measured. No delivery actions performed. Skill resolution: `paths-injected` for all four assigned skills.

## Resume — preservation infrastructure failure

Ownership conflict resolved by explicit transfer to executor. Exact active-attempt continuation returned `proceed`; no new attempt, reset, or rescope was requested. CodeGraph exploration timed out, so targeted reads were used. Global strict-TDD guidance was read.

Preservation command: `node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts` exited **1**: 44 passing native adapter tests; selection test module failed to load with `ERR_MODULE_NOT_FOUND` for `@earendil-works/pi-tui` imported by `extensions/gentle-ai.ts`. This is infrastructure failure, not behavioral RED. Named passing assertions include exact selected-root argv/validated v2, malformed identity/dependency/instruction rejection, optional instruction omission, and fail-closed JSON/timeout/nonzero results. Selection assertions did not execute.

Full output: `/tmp/pi-unit1-preservation.log`; SHA-256 `ebfd158112576b27cfa4f3e73dc27d2873ca8e9d89bfa2a3cbb447f6664caa4d`. No tests authored; no production or asset changes. Strict TDD baseline gate stopped implementation. No dependency repair outside allowed surfaces attempted.

| Task | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 2.1–2.3 | 44 pass; selection module import failed | Not reached | Not reached | Not reached | Not reached |

`git diff --check` exited 0. `git diff --numstat` was empty (tracked changes only); `git status --short` showed the existing untracked change directory. This does not count untracked planning content as zero lines. Required three-file focused run, `pnpm test`, and runtime module check were not reached; producer was not invoked and its identity remains unverified.

One failed-outcome settlement was submitted with the actual log digest and truthful process/cleanup facts. Native settlement returned **blocked**, reason `undeclared_untracked`, naming this apply-progress file as outside candidate selection. No settlement completion claimed; no automatic retry/reset/rescope. Parent must reconcile the native requested untracked ruling before settlement can complete.

Tasks 2.1–2.3 remain unchecked, as do all other previously pending tasks. No accepted AI state changed. Only this progress file changed; runtime test process exited, no managed child/live provider launched. Rollback remains limited to this newly created progress file; preserve native history and all prior planning artifacts. No source rollback was executed or required. Next: explicit settlement reconciliation and authorized dependency provisioning before another admitted implementation run.

## Correction attempt — baseline restored; human-scope input unresolved

Historical settlement/dependency blockers above are superseded by the parent's reconciliation and offline provisioning. Current status confirmed the correction attempt; exact continuation returned `proceed`. No new attempt/reset/rescope or index changes were made. Consumed the supplied authoritative v2 status (apply ready, repo-local allowed workspace, no blockers), re-read proposal/specs/design/tasks/config and global strict-TDD guidance. All four injected skills loaded. CodeGraph successfully inspected current handlers and surface validation.

Preservation now passes. Exact commands and evidence:
- `node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts tests/sdd-status-continue.test.ts` exited 0: 55 passing tests; the last named path does not exist and contributed no tests. Named assertions cover selected native archive transport without local fallback, selected malformed identity/root/dependency/instruction refusal, adapter strict v2 and process errors. Log `/tmp/pi-unit1-resume-preservation.log`, SHA-256 `2418ebf1959bd023fe971acc4d4d490decb90bb1a49b03ec378a8d391b4d0512`.
- `pnpm test` exited 0: 2143 pass, 9 skipped, zero failures; provider mirror check and runtime harness also exited 0. Log `/tmp/pi-unit1-resume-suite.log`, SHA-256 `f17beba28fba562e478c4a770b9da6d294821abcdd2256cf7a1a74f77f1e7618`.
- `pnpm run check:runtime-modules` exited 0 (six generated modules match); log `/tmp/pi-unit1-resume-runtime.log`, SHA-256 `8efd974e26e4e463a618f2429ec1366773de22e010cc9deef590f4d1bdcb1403`.
- `git diff --check` exited 0 before this append. Controlled producer `sdd-continue --help` rejected `--help`; no fixture execution or continue argv/result proof is claimed.

Implementation paused before behavioral RED: `handleSddContinueCommand` currently has no identified input carrying current human read-only/excluded-marker scope. The existing worker Markdown surface validator checks delegated task/context syntax, not current session authorization. Native `actionContext` alone is expressly insufficient; a fresh UI confirmation without scope intersection could override prior exclusions. Parent was asked to identify the existing carrier or approve narrow human-scope transport within `extensions/gentle-ai.ts`; no answer arrived before stopping. Do not invent a scope authority or claim the required negative proofs.

| Task | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 2.1–2.3 | 55 focused pass; full baseline pass | Not authored | Not reached | Not reached | Not reached |

No implementation completed and no task checkbox changed: 2.1–2.3 and all other pending tasks remain unchecked; accepted AI checks retained. No Pi unit 2 work. Only this cumulative progress append is new. All foreground commands exited; no live provider or managed actor was launched by this executor (the package test harness ran). No production rollback required; rollback this append only, retaining previous evidence, dependencies, index state and native history. Unit boundary remains 2.1–2.3, one eventual size-exception Pi PR, no delivery actions; native hard cap remains 400. Interrupted settlement was submitted with truthful diagnosis/process/cleanup and no evidence revision. Exact compact settlement result: `state: blocked`, `reason: maintainer_decision` (attempt or changed-line budget needs a maintainer decision). No reset/rescope/retry was performed; this is not successful correction or permission to resume.

## Pi unit 1 — implemented under the fresh post-reset continuation

Consumed supplied authoritative v2 status: apply ready, repo-local workspace/allowed root equals this worktree, no blocked reasons or action-context warnings. Re-read all local inputs/config and four injected skills; strict TDD active. Exact active continuation returned proceed; no new attempt, reset, rescope, index mutation or token persistence. Prior scope ambiguity is resolved: per-call exact canonical marker-path confirmation is the human grant, not native actionContext. No persistent/source authority is created.
Tasks 2.1, 2.2 and 2.3 were checked immediately after their respective preservation, RED and final GREEN completion; six accepted AI checks retained. Final task readback required before return. Pi units 2–4 untouched. Independent unit acceptance remains pending; this is not final change verification.

### TDD Cycle Evidence
| Task | Preservation / RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| 2.1 | Initial focused 55 pass and pnpm test exit 0; producer preservation then 56 pass | Real producer repeated status byte equality, seven dependencies/four groups, missing marker and selected recommendation asserted; preservation-only, not defect RED |
| 2.2 | Focused RED exit 1, six genuine failures: missing continue adapter, nullable discovery, native command rendering, exact confirmation, denied/headless scope, malformed recommendation refusal | The same cases pass; undefined-confirmation test default was corrected to actually supply undefined, and malformed-null refusal accepts the decoder's expected-string error |
| 2.3 | Startup/managed-guidance triangulation RED exit 1 (two failures); unsafe planning-context RED exit 1; runtime harness RED pinned old schema | 66 focused pass; direct producer → native adapter → registered handlers records status/status/continue only; all twelve tokens, optional groups, marker-only empty source roots, wrong workspace, missing UI, symlink, cancellation and misleading prose covered. Shared sddProjection consolidates transport without routing logic; final reruns pass |

Exact commands (cwd: this worktree), logs and exits:
- `node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts`: preservation logs `/tmp/pi-unit1-final-preservation.log` exit 0; RED `/tmp/pi-unit1-red.log` exit 1. Initial preservation command also named the absent third path, contributing no tests; subsequent commands use only existing files.
- `SDD_TEST_PRODUCER=/tmp/sdd4484-r3-independent.lipiel6e/runner-temp/gentle-ai node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts`: final `/tmp/pi-unit1-verified-focused.log`, exit 0, 66/66; SHA-256 `5c044ea82d2c95efdf80590edc379db1c0f61b2b691225dcd875ccd1d902fb27`.
- `SDD_TEST_PRODUCER=/tmp/sdd4484-r3-independent.lipiel6e/runner-temp/gentle-ai pnpm test`: final `/tmp/pi-unit1-verified-suite.log`, exit 0, 2154 pass/9 skipped plus provider mirror and runtime harness; SHA-256 `0b0f1664f140fbda5cca54c66cd7736ce0521d122316b2d51f48b470df6f3582`. Earlier `/tmp/pi-unit1-suite-green.log` exit 1 exposed old harness assertion at line 495; authorized migration preserved unrelated assertions.
- `pnpm run test:harness`: `/tmp/pi-unit1-harness-green.log`, exit 0. `node scripts/build-runtime-modules.mjs` initially exited 1 (requires mode); corrected `node scripts/build-runtime-modules.mjs --write` exited 0. Only authorized runtime/native-review-cli.mjs changed.
- `pnpm run check:runtime-modules`: stale-mirror RED exit 1, final `/tmp/pi-unit1-verified-modules.log` exit 0; SHA-256 `7736a80e8adfe05d4c232347dc9f1a207cd27058270f7c7ba70a6ebca37790cd`. `git diff --check` exit 0.
Producer binary SHA-256: `12f33cbbbeb11000e452f435e27a39c61d5ebb354c544063b2a843d624628d73`. Controlled fixtures are removed by test teardown; foreground commands exited. Process-list check for focused tests, runtime harness and controlled status/continue found no surviving process. No live provider, installation, Docker or delivery operation launched.

Changed behavior/tests/guidance: lib/native-review-cli.ts, extensions/gentle-ai.ts, assets/agents/sdd-apply.md, tests/native-review-cli.test.ts, tests/sdd-selection-transport.test.ts; additionally parent-authorized tests/runtime-harness.mjs and generated runtime/native-review-cli.mjs. Existing lib/sdd-status.ts rendering sufficed unchanged; deliberate local sync retained. New test file unnecessary: existing registered-command/hook seams cover the required assertions without undeclared untracked files.
Rollback boundary: revert only this unit's adapter/generated mirror, handlers, guidance and paired test hunks together, preserving earlier planning, native history, dependencies and index state. No production rollback executed; fixture teardown is verified. One eventual size-exception Pi PR, no chain or delivery; native cap 400 unchanged. Pre-progress incremental count was 333 A+D against admitted tree (includes generated mirror and task edits); final count is measured after this append and reported with actual settlement response in the phase envelope.
Passing settlement must bind prior failed evidence `sha256:ebfd158112576b27cfa4f3e73dc27d2873ca8e9d89bfa2a3cbb447f6664caa4d` to the distinct fresh suite digest above. No settlement success is assumed here; actual compact result belongs to the returned envelope. Independent verifier must inspect this corrected candidate before advancing units.

### Remaining exact unchecked task lines
- [ ] **Start → end:** Start with closed native declaration/admission and the currently supported managed route; end with separate preservation cases for exact class grants, matching/one-sided/missing-intent recovery, strict stale/divergent refusal, confirmed proposal handoff that does not interview, and automatic unresolved choices emitted once as a lossless grouped prompt—without encoding a whole-phase tool denial as desired behavior.
- [ ] **Start → end:** Start with failing cases for documentation/open-web exact selected grants, inactive/missing extension tools, separately authorized read/write/Engram persistence, narrowed OpenSpec path, wrong worktree, and denial persistence; also require missing-tool denial → corrected capability/artifact facts → continuation with the **same** bounded selected-store path/scope, never a broadened or replacement scope. End with existing launch data carrying selected classes/per-class grants and actual extension selection, with child-local inventory recheck.
- [ ] **Start → end:** Start with RED tests requiring `remediate` to refuse if unsupported, carry `failedEvidenceRevision` unchanged when supported, acquire once, and settle pass/fail/interruption through existing compact JSON; end with narrow adapter methods and runner finalization using retained session/task history, not a second ledger.
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Pi unit 2 — blocked before implementation

Consumed `/tmp/sdd4484-pi-unit2-status.json`: native v2, apply ready, 9 complete/5 pending, no blockers; repo-local workspace and allowed root match this checkout. Read proposal, all specs, design, tasks, config, previous progress, injected skills and global strict-TDD guidance. The malformed injected `.md` skill entry was not a resolvable skill; other four paths loaded. No additional phase skill path was supplied. Exact active-attempt continuation returned `proceed`; no new attempt/reset/rescope or index mutation. Token not retained in artifacts.

Supported installed Pi extension documentation was read completely: `getAllTools().sourceInfo.path` supplies extension provenance and `-e/--extension` loads an explicit selection; tool metadata does not expose invocation of arbitrary registered Engram tools. CodeGraph explore timed out; targeted reads used. No extension/subagent examples or further linked docs were consumed because implementation stopped before that gate.

Blocker: `buildRequest` currently receives model task parameters, agent configuration and tool inventory, not a trusted orchestrator-owned selected research state/current narrow host path authorization accessor. Parent clarified semantic owners (native status, orchestrator state, ordinary host authorization), but supplied no existing callable carrier for the latter two. Model-supplied revision/readback/authorization fields cannot establish those facts. Independently fetching Engram readbacks through metadata alone is unsupported. Implementing a new persistence invoker or trusting transported assertions would violate this unit's no-store-engine/no-new-authority boundary. Need an exact existing trusted carrier/accessor or a maintainer-scoped design adjustment before behavioral RED and production edits.

| Task | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| Pi 3.1, 3.2 | 116 focused pass; live test skipped by opt-in guard | Not authored | Not reached | Not reached | Not reached |

Commands: `node --experimental-strip-types --test tests/sdd-research-capabilities.test.ts tests/gentle-agents.test.ts tests/agents-runner.test.ts` exit 0, 116/116 (`/tmp/pi-unit2-preservation.log`); `node --experimental-strip-types --test tests/sdd-research-live.test.ts` exit 0, 0 pass/1 skipped, no provider execution; `git diff --check` exit 0 before this append. `pnpm test` and `pnpm run check:runtime-modules` not run: implementation prerequisite unresolved. No spawn argv/env changes or controlled selected-extension traces claimed. No behavioral store/path/worktree equality proof added.

Only this cumulative progress append changed. Tasks re-read: 3.1, 3.2, 4.3, 5.1 and 6.1 remain unchecked, exact lines preserved in the preceding section; 9 existing checks unchanged. No completion claimed and no checkbox update needed. Unit 1 preserved. Rollback: remove only this append, retaining all prior progress, source, native history and index. All foreground tests exited; process inspection found only the inspecting shell, no surviving test runner. No managed actor, live provider, installation or delivery launched.

Boundary remains Pi 3.1/3.2, hard native cap 400, eventual single size-exception PR/no chain. Forecast 300–365 was parent-provided, not implementation-validated. Git diff against admitted tree misleadingly lists untracked planning files as deletions; do not count those as edits. Interrupted settlement will record this unresolved trusted-input boundary without evidence revision; its actual compact response is reported in the phase envelope, not presumed here.

### Pi unit 2 clarification and workload stop

Parent's subsequent correction resolves the trusted-carrier concern above: handoff fields are untrusted narrowing intent; actual child tools/readbacks and ordinary host authorization remain authoritative. No `verified` field or host-side Engram invocation is needed. Read the installed complete subagent README and launch example after that correction. No production edits or behavioral RED followed.

One honest actual-surface reforecast, including native-counted generated mirrors: lib validation/selected-grant/provenance/canonical-path/topic restrictions 125–155 A+D, corresponding generated mirror 100–130; runner transport 15–25 plus mirror; extension parameter/run/continue/hooks 45–65; genuine behavioral tests 100–130; paired guidance/progress 35–45. Smallest complete estimate is at least 435 A+D, above the hard native cap 400. This is a forecast, not measured implementation. Do not compress code or drop assertions to fit. Parent was asked for a narrower acceptance boundary; query timed out. Stopped before implementation; requires parent/maintainer workload decision, not an automatic reset/rescope or size-exception inference. Eventual PR exception does not raise the native cap.

Remaining tasks and rollback unchanged. No runtime/store/extension trace acceptance claimed. Terminal interruption records the budget stop and successful preservation only; actual compact settlement response belongs to the returned envelope.

## Pi unit 2A — selection/extension transport implemented, not accepted

Consumed parent authoritative native v2 status: OpenSpec/apply ready, repo-local workspace and allowed root equal this checkout, 9 complete/5 pending, no action-context warnings. Read proposal, all specs, design, tasks, config and cumulative progress directly; four injected skills and global strict-TDD guidance loaded (`paths-injected`). Exact active continuation returned proceed; no new attempt/reset/rescope or token persistence. CodeGraph timed out; targeted reads and complete installed extension documentation/permission-gate example supplied API context. No child subagents delegated.

Scope: selected documentation/open-web entries carry exact per-class tool lists and existing tool-to-sourceInfo.path mappings as untrusted intent. Host intersects with registered/active/non-SDK/agent-restricted inventory; only complete selected classes provision research tools and matching --extension paths. Partial observed per-class grants remain visible without permitting incomplete collection. Existing TaskRequest, runner argv/env and run/continue parameters carry narrowing; missing fresh selection cannot inherit research. Child rechecks registration, activity, non-SDK provenance, exact path and allowlist at calls. Separately authorized local/Engram tools and parent messaging remain restricted and usable. No store/path/topic/worktree/revision transport, write enforcement, denial persistence/re-entry or store readback was added; these remain 2B.

### TDD Cycle Evidence
| Boundary | Preservation | Genuine RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|---|
| Pi 3.1 preservation / selected grants | Initial three-file focused run 116/116; live probe 1 opt-in skip | /tmp/pi-2a-red-cap.log: documentation incorrectly received all four routes | Exact documentation, open-web and both-class selection; missing each tool, inactive, SDK, restricted, missing/unknown grants, gateway substitution and eight local/persistence tools pass |
| Host → runner → child | Existing runner/child tests passed | /tmp/pi-2a-red-transport.log: missing transport and unselected child route allowed | Actual registered subagent_run fake-spawn argv/env, exact extension selection, separately authorized parent message, child wrong-path/SDK/inactive/unregistered/restricted refusal pass; runner replaces stale env and preserves spaced paths |
| Negative triangulation / guidance | Existing per-class rendering preserved | /tmp/pi-2a-triangulate-red.log: inherited object key accepted; /tmp/pi-2a-partial-red.log: partial observations lost; /tmp/pi-2a-guidance-red.log: shipped selection contract missing; /tmp/pi-2a-continue-red.log: continuation schema lacked selection | Own-key validation and partial observations corrected; shared run/continue schema consolidates shape; missing continuation selection removes research paths/tools; final focused rerun 120/120 |

Four new tests plus migrated existing preservation/transport assertions; unit and registered-hook/fake-spawn integration layers, not real provider execution. Shared researchAgent performs both host and child checks; no new engine or additional refactor warranted. No whole-phase local/persistence denial was encoded as desired behavior.

Final commands, all from this worktree:
- `node --experimental-strip-types --test tests/sdd-research-capabilities.test.ts tests/gentle-agents.test.ts tests/agents-runner.test.ts`: exit 0, 120 pass; /tmp/pi-2a-final-focused.log, SHA-256 fe9c43afa6ad25d2f9ca1984ec88da3d82c5b7856d2e4437c73f933da9136331.
- `pnpm test`: exit 0, 2157 pass/10 skipped/0 failed, provider mirror and runtime harness pass; /tmp/pi-2a-final-suite.log, SHA-256 10e2181f32104f266f447a63ac477556fd6d259c206d38f07bd87c836d28e90f. The additional producer-bound unit-1 test is skipped without its opt-in producer environment; no fresh producer proof claimed.
- `node --experimental-strip-types --test tests/sdd-research-live.test.ts`: exit 0, one opt-in skip/no provider; /tmp/pi-2a-final-live.log, SHA-256 59ff0c4256cf8f151e512ec572e1d1461df88dc8b5efa57ecd4bd4b4110c5008.
- `pnpm run check:runtime-modules`: exit 0, six existing generated modules match; /tmp/pi-2a-final-modules.log, SHA-256 547e8dd09a93700e1f04297815d822cf14fabbc5ea2a6ff1f57c985bd242f6e2. `git diff --check`: exit 0.

Reforecast correction: runtime/sdd-research-capabilities.mjs and runtime/agents-runner.mjs do not exist and are excluded from the authoritative generator; parent explicitly directed omission. These modules load directly as TypeScript. No generator or generated bytes changed, no new files. Changed files: lib/sdd-research-capabilities.ts, lib/agents-runner.ts, extensions/gentle-agents.ts, assets/agents/sdd-research.md, tests/sdd-research-capabilities.test.ts, tests/gentle-agents.test.ts, tests/agents-runner.test.ts, tests/sdd-research-live.test.ts and these tasks/progress artifacts.

Tasks: added a bounded progress note; no checkbox completion claimed. Parent explicitly retains 3.1/3.2 unchecked pending combined unit-2 independent verification after 2B; nine existing checks preserved. All five exact unchecked lines in the earlier Remaining section remain unchanged (3.1, 3.2, 4.3, 5.1, 6.1). No Ready-for-verify claim.

Rollback boundary: revert only 2A hunks in the eight behavior/test/guidance files above and its task/progress notes together, preserving unit 1, adopted planning, index state and native history. No production rollback executed. Tests remove fixtures and cancel fake children; all foreground commands exited, process inspection showed only its inspecting shell and no surviving test/harness runner. No managed actor/live provider, global install/download, Docker or delivery operation launched.

Workload: one bounded part of functional Pi unit 2, one eventual size-exception Pi PR; no independent 2A acceptance/advancement. Native hard cap remains 400 A+D despite clarified future product sweet-spot policy. Final incremental count includes these artifacts against admitted tree 02a4b3237ff9bbb757c453509929b8375e4cbd9c and is reported with terminal settlement in the phase envelope. Passing settlement binds the fresh suite digest above; actual compact response is reported separately, never presumed by this record.

## Pi unit 2B — workload stop before RED

Consumed `/tmp/sdd4484-pi-unit2b-status.json`: authoritative native v2, OpenSpec, apply ready, 9 complete/5 pending, no blockers; repo-local workspace/allowed root matches this checkout. Read proposal, all specs, design, tasks, config, cumulative progress, all four injected skills and global strict-TDD guidance. No local strict-TDD override exists. Exact active-attempt continuation returned proceed; no new attempt/reset/rescope or token persistence. CodeGraph timed out; targeted source/test reads used. Skill resolution: paths-injected.

The narrowed 2A transport exists, but child tool_call has no artifact argument restriction or actual readback observer; continuation currently retains cwd/sessionPath, not bounded artifact intent. This is implementation scope, not a missing authority accessor: carried facts remain untrusted narrowing intent and ordinary host permissions remain authoritative. No host persistence invoker/store engine is proposed.

One honest 2B reforecast, excluding nonexistent generated mirrors: scope/path/topic/readback validation 130–155 A+D; transport/retained continuation/child hooks 85–100; genuine behavioral tests 175–200; paired guidance/tasks/progress 40–45. Total **430–500 A+D**, minimum 30 above the active hard 400 limit. Parent confirmed no existing seam removes that gap and directed pre-RED stop/terminal interruption, retaining the complete acceptance scope. A future audited admission is parent/maintainer-owned; product simplicity policy does not enlarge this attempt. No compression, omitted tests, source edits or manufactured RED.

### TDD Cycle Evidence
| Tasks | Preservation | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 3.1 / 3.2 combined unit | 120 focused pass; full baseline passes | Not authored: workload gate | Not reached | Not reached | Not reached |

Commands executed from this checkout:
- `node --experimental-strip-types --test tests/sdd-research-capabilities.test.ts tests/gentle-agents.test.ts tests/agents-runner.test.ts`: exit 0, 120 pass; `/tmp/pi-2b-preservation.log`, SHA-256 `8a3a91ba70aa07cbd351e894e9747cc68d36b0d9860c6730805256965f121134`.
- `node --experimental-strip-types --test tests/sdd-research-live.test.ts`: exit 0, one opt-in skip, no provider execution; `/tmp/pi-2b-live.log`.
- `pnpm test`: exit 0, 2157 pass/10 skipped/0 failures, provider mirror and runtime harness pass; `/tmp/pi-2b-suite.log`, SHA-256 `070888969f2670a84b93360389f772483e3525a29e11eb3ee2b91aa779e5ce05`.
- `pnpm run check:runtime-modules`: exit 0, six modules match; `/tmp/pi-2b-modules.log`. No generation performed. `git diff --check`: exit 0 before this append; rerun before return.

No implementation completed: tasks 3.1, 3.2, 4.3, 5.1, 6.1 remain unchecked, with their exact lines retained in the earlier Remaining section. Nine existing checks retained; tasks file unchanged, and no false 11-complete prose update. Combined unit 2 remains incomplete; independent verification/review and installed uptake remain parent-owned. The old adoption prose is historical, not current task-count evidence.

Only this progress append changes candidate bytes. Rollback removes this append alone, preserving unit 1/2A, tasks, native history and index. Foreground commands exited; no live provider/managed actor, installation, generated file, new source file, delivery or commit. Process inspection and admitted-tree byte comparison are recorded in terminal response. Boundary remains the same bounded 2B scope within one eventual size-exception Pi PR; no PR or chain created. Interrupted settlement omits evidence revision; actual compact settlement state is reported in the phase envelope, not assumed here.

## Pi unit 2B — implemented; combined unit 2 awaits independent review

Consumed `/tmp/sdd4484-pi-unit2b-status.json`: authoritative native v2, OpenSpec/apply ready, 9 complete/5 pending, repo-local workspace and allowed root equal this checkout, no action-context warnings. Read local proposal/specs/design/tasks/config/progress, four injected skills and global strict-TDD guidance; CodeGraph succeeded. Read complete installed extension docs and permission-gate example. Exact active continuation returned proceed under audited expanded unit 2B (one attempt, 500 A+D); no new attempt/reset/rescope/supersede, token persistence, index or delivery action. Skill resolution: paths-injected.

Completed Pi 3.1 and 3.2 implementation with settled 2A: exact store/worktree/change-local paths and Engram id/project/topic/revision/content narrowing, retained denial intent in existing task history, same-scope continuation with fresh inventory, child-local tool argument restriction and actual readback observation. Host permissions remain independent. Full write/save call bytes are transient expected post-write intent, never durability; successful results require newer actual readback, hybrid byte identity, and separate research/decision/native admission. Stale/divergent state refuses recovery writes; none cannot become ready. Tasks 3.1/3.2 were checked after focused GREEN; persisted prose now states 11 complete/3 pending. Independent combined-unit review is parent-owned; real installed uptake remains 5.1.

### TDD Cycle Evidence
| Tasks / boundary | Preservation | Genuine RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|---|
| 3.1 / 3.2 | 120 focused tests pass | Missing exported bounded-path/readback functions; registered hook allowed outside writes; runner omitted artifact transport | 123 focused pass: exact paths/topics, wrong worktree, stale/broadened scope, missing intent, none, missing-tool correction, active-tool denial |
| Actual persistence/readback | Existing child/runner baseline preserved | Post-write readback expected old bytes; broken symlink write allowed; stale/divergent recovery write allowed; differing hybrid writes did not error; mutable request changed launch scope | Full-write/save observations, success then actual dual-store readback, failed-write invalidation, malformed/wrong metadata, exact digest/revision, retained task/launch snapshots pass |
| Guidance / refactor | Existing asset/migration tests initially passed | New guidance absent; full suite exposed historical asset reconstruction hash mismatch | Guidance paired; shared write identity type/indentation refinement preserves GREEN; exact historical reconstruction fixed without changing legacy hash/manifest |

Commands from this checkout: `node --experimental-strip-types --test tests/sdd-research-capabilities.test.ts tests/gentle-agents.test.ts tests/agents-runner.test.ts` preservation exit 0 (120); final same three files exit 0 (123), `/tmp/pi-2b-expanded-focused.log`, SHA-256 `0e6f68d8e585dbce36b025515009f596102a5344ca35a786935c08f6cc89a8b8`. Behavioral RED logs `/tmp/pi-2b-expanded-{red,hook-red,transport-red,write-red,symlink-red,triangulate-red,stale-red,dual-red,retained-red,guidance-red}.log` all exit 1. Export-missing RED is module-load failure; subsequent named assertions are behavioral RED, not infrastructure failures.

Final `pnpm test` exit 0: 2160 pass, 10 skipped, provider mirror and runtime harness pass; `/tmp/pi-2b-expanded-suite.log`, SHA-256 `06ad3013c7469a12d528b742b357b25387791a42ad18d67f18d6b7a1761b5035`. Earlier full run failed solely `tests/package-manifest.test.ts:652` because the new section survived legacy reconstruction; parent authorized exactly one added replacement there. Immutable old hash/manifest unchanged. `node --experimental-strip-types --test tests/package-manifest.test.ts` exit 0, 47 pass (`/tmp/pi-2b-expanded-manifest.log`). `pnpm run check:runtime-modules` exit 0, six modules match (`/tmp/pi-2b-expanded-modules.log`); no generation. `node --experimental-strip-types --test tests/sdd-research-live.test.ts` exit 0, one opt-in skip (`/tmp/pi-2b-expanded-live.log`), no provider/managed actor. `git diff --check` exit 0. Full failure output was console-observed; suite log now contains the passing rerun.

Changed files: lib/sdd-research-capabilities.ts, lib/agents-runner.ts, extensions/gentle-agents.ts, assets/agents/sdd-research.md, tests/sdd-research-capabilities.test.ts, tests/gentle-agents.test.ts, tests/agents-runner.test.ts, parent-authorized tests/package-manifest.test.ts, tasks.md and this cumulative progress. No new files, generated modules, installation/downloads, Docker, live providers or commits. Deviation clarified by parent: arbitrary edit patches cannot predict full content; use explicit full bounded write/save and actual readback. Unsupported Engram metadata and non-JSON/truncated artifact bytes fail closed. No evidence validator, host persistence invoker or second store/permission ledger was introduced.

Rollback boundary: only this unit's paired source/test/guidance hunks and task/progress changes, preserving unit 1, settled 2A, historical evidence, native records and index. No production rollback performed. Controlled hook/fake-spawn tests clean fixtures and fake children; all foreground commands exited, process inspection found no surviving test/runtime-harness process. This is not installed producer → Pi → managed-child execution proof. Workload: bounded 2B completes functional unit 2 with 2A, eventual single size-exception Pi PR, no PR boundary/delivery action; 500 is only this audited cap, not a spending target. Incremental A+D against admitted tree `48840eb8626c231ebeba42c5e0381e9309a12145` is measured after this append in the phase envelope. Settlement uses the passing suite digest above; actual native terminal state is reported separately, not presumed here.

### Remaining exact unchecked task lines
- [ ] **Start → end:** Start with RED tests requiring `remediate` to refuse if unsupported, carry `failedEvidenceRevision` unchanged when supported, acquire once, and settle pass/fail/interruption through existing compact JSON; end with narrow adapter methods and runner finalization using retained session/task history, not a second ledger.
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Pi unit 3 — confirmed workload stop before RED

Consumed injected authoritative native v2 status: OpenSpec/apply ready, 11 complete/3 pending, repo-local workspace and allowed root match this checkout, no action-context warnings. Read local proposal, all specs, design, tasks, config and cumulative progress, four injected skills and global strict-TDD guidance. Read `/tmp/sdd4484-pi-unit3-acquire.json`; exact active continuation returned proceed under one attempt / 740 A+D. No new attempt/reset/rescope/supersede or token persistence. CodeGraph timed out; targeted reads used. Extension documentation was partially read only; no implementation followed. Skill resolution: paths-injected.

Parent resolved the evidence-owner question by authorizing a narrow remediation-only carrier in existing task/history: retain exact verification commands/cwd, harness command or concrete prior N/A, rollback boundary and inspection command; runner observes paired tool events, exit codes and bounded concrete results/digests. Neither final prose, ChildObservationSnapshot nor mutation callbacks establish passing correction. Missing/unmatched/incomplete/nonzero/truncated/unsupported evidence cannot pass; failure/interruption remains truthfully settleable without successful verification.

Complete pre-RED reforecast including generated native adapter mirror: adapter types/validation/flags/mirror 190–240 A+D; typed selection/ownership/package/launch checks 80–110; command-plan observations/task-history 145–185; durable pre-settle persistence/async finalization/exact replay/spawn cleanup 115–150; behavioral tests 230–290; asset/tasks/progress 35–50. Total **795–1025 A+D**, minimum 55 above hard 740. Parent confirmed stopping with no source/test edits. This is an estimate, not implementation measurement; no compression, omitted negatives or raised budget. Eventual single size-exception PR does not enlarge the native cap.

### TDD Cycle Evidence
| Task | Preservation | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 4.3 | 242 pass / 1 opt-in skip | Not authored: confirmed workload stop | Not reached | Not reached | Not reached |

Executed `node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts tests/agents-runner.test.ts tests/gentle-agents.test.ts tests/sdd-managed-runtime-settlement.test.ts tests/sdd-agent-tools.test.ts tests/delegated-key-learnings-contract.test.ts tests/package-manifest.test.ts`: zero failures, 243 tests, 242 pass/1 skip; `/tmp/pi-unit3-preservation.log`, SHA-256 `801e22272f98b41116910187b6ba2bcff11997706ed2c11f8b59e5e21dedf61b`. Proposed settlement test file does not yet exist and contributed no tests. No behavioral RED, native-remediation boundary proof or implementation acceptance claimed. Accepted binary acquire/settle help read; native diagnostic status confirms admission tree `ba92fdccdef4543b2ff030fa9bb3f38ef37f3bed`. `git diff --check` exit 0; process inspection found no surviving node test/runtime-harness process. Full suite, runtime mirror and package verification were not run after this pre-implementation stop.

Only this bounded cumulative append changed. No task completed or checkbox changed: persisted tasks remain 11 checked/3 unchecked; units 1–2 and stale-artifact protections preserved. Rollback removes only this append, preserving prior source/planning/index/native history. All foreground processes exited; no managed actor/live provider, downloads, installation, generation, branch/commit/PR or delivery. Interrupted settlement omits evidence revision and records preservation/workload/process/cleanup facts; actual native terminal response and exact incremental count are reported in the phase envelope. No automatic recovery is authorized.

### Remaining exact unchecked task lines
- [ ] **Start → end:** Start with RED tests requiring `remediate` to refuse if unsupported, carry `failedEvidenceRevision` unchanged when supported, acquire once, and settle pass/fail/interruption through existing compact JSON; end with narrow adapter methods and runner finalization using retained session/task history, not a second ledger.
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Pi unit 3 — implemented under expanded admission; independent review pending

The preceding 22-line workload-stop evidence is preserved unchanged. Consumed parent authoritative OpenSpec/apply-ready v2 status and expanded generation-8 work unit `pi-remediation-runtime-expanded`: one attempt, 1025 A+D maximum, same repo-local workspace/allowed root and explicit size exception. Exact active continuation returned proceed; no reset/rescope/supersede, index change or new objective by executor. Local proposal/specs/design/config/tasks/progress were read directly during this unit and unchanged planning inputs checked against admission tree `8b0d1f0782ae2c7c32fe89810f9660924dbc0d8a`. All four injected skills and global strict-TDD guidance loaded; CodeGraph timeout led to targeted reads. Installed extension documentation, stock shell source and override/permission examples informed the wrapper. Skill resolution: paths-injected; no action-context warning.

Task 4.3 is checked immediately after final GREEN; tasks readback must show 12 complete/2 pending. Typed remediation now carries unchanged failed-evidence identity through selection, native status checks, launch request, task/history, acquire and settle. Missing owner/asset/capability and stale identity refuse before acquire; blocked/complete admission spawns nothing. Exact optional prior token and untracked selection remain native inputs. No apply alias, local-sync change, research broadening or review authority was introduced.

The parent-authorized remediation-only plan carries exact commands/cwd, harness command or concrete prior N/A, and rollback boundary/inspection. SDK stock bash does not expose numeric exits and accepts null as non-error; the authorized remediation-child-only wrapper captures actual local operations results while preserving schema, rendering, error behavior, truncation/full-output metadata, abort signal and process-tree cleanup. Paired runner events retain numeric exits and bounded output/digests; malformed/missing/mismatched/nonzero/null/truncated/incomplete evidence cannot pass. No assistant prose, response-metrics snapshot or mutation callback is success evidence. The plan reaches the child without its admission token. Separately authorized memory artifact tools remain declared.

Runner finalization waits for observed process cleanup, records failed/interrupted outcomes without verifier-success prerequisites, and closes spawn/no-PID failures without sending another actor prompt. Exact token/request ID/payload and observations are saved in existing task history before settlement; one ambiguous reply permits one identical replay only. Unresolved records survive pruning; refused/uncertain or prose-only completion is not advertised as successful correction. Native SDD mutation uncertainty does not redirect into review status. Native settlement remains authoritative; no new ledger, recovery engine, command or protocol.

### TDD Cycle Evidence
| Boundary | Preservation / genuine RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| 4.3 adapter/selection | Expanded preservation 242 pass/1 skip; missing compact methods and rejected typed remediation selection | Exact argv, optional empty CAS/prior token, unchanged failed revision, malformed response/evidence, stale identity and review separation pass |
| Evidence and runner | Export-missing observation RED; missing finalizer, accepted malformed content, no-PID prompt, lost failure observations, missing plan prompt and history pruning RED | Exact paired commands/cwd, actual zero/nonzero/null, harness/N/A, rollback, bounded/truncated/malformed results, cancellation and synchronous/no-PID failures pass |
| Durable bracket | Missing admission function/owner and real registered-tool remediation rejection; later altered actor acceptance and terminal/prose status RED | One acquire, blocked/complete no settlement, exact one replay, save-before-call, persistence failure, two lost replies, revision drift and single finalization pass |
| Installed asset / isolation | Owner/package catalog and explicit verification-list RED; full suite exposed actor-count and closed telemetry taxonomy fixtures | Actual isolated installation/readback, stock wrapper real shell execution/abort and fake-child traces pass; non-remediation bash unchanged; telemetry remains dark |
| Refactor | All focused scenarios green before refactor | Shared schema/plan types, explicit callback arguments and native-only render typing; focused/full reruns pass |

Export-missing cases are compile/API RED, not behavioral assertions. The first bracket fixture accidentally named nonexistent `parseAgentFile`; corrected to existing `parseAgentDefinition` before treating its missing-asset/API failures as evidence. No failed baseline was presented as preservation success. Primary logs: `/tmp/pi-unit3-{adapter,selection,observation,shell,admission,bracket,runner,wiring,triangulate,nopid,retention,owner,terminal,prose,cli-edge,schema}-red.log` where created; individual GREEN logs and final logs retain actual results. Shell/child registration and synchronous-spawn additions that passed first execution are coverage-gap closure, not defect RED.

Final commands (this checkout), all exit 0:
- `node --experimental-strip-types --test tests/native-review-cli.test.ts tests/sdd-selection-transport.test.ts tests/agents-runner.test.ts tests/gentle-agents.test.ts tests/sdd-managed-runtime-settlement.test.ts tests/sdd-agent-tools.test.ts tests/delegated-key-learnings-contract.test.ts tests/package-manifest.test.ts`: 280 pass/1 opt-in skip, `/tmp/pi-unit3-focused-final.log`, SHA-256 `e969b98dda3e66b7bf346c69c698b5dc898a22b8013c74770f63d841d9c05e68`.
- `pnpm test`: 2198 pass/10 skipped, provider contract mirror and runtime harness pass; `/tmp/pi-unit3-suite-final.log`, SHA-256 `697ba6449ebe8ccf4869ec4c835cc70f8508f4af957e99ce0bc5c9482b9c1c60`. Earlier full runs exposed new actor installation counts and the telemetry taxonomy fixture, not production telemetry defects.
- `pnpm run check:runtime-modules`: six mirrors match, `/tmp/pi-unit3-modules-final.log`, SHA-256 `902b123672ecb07a43806d13dc565ec97ad5a489e3ce9dbcc45d5f0514a8392e`. `node scripts/build-runtime-modules.mjs --write` changed only the authorized native-review-cli mirror.
- `node scripts/verify-package-files.mjs`: 169 files and 69 exact byte-pinned artifacts pass, `/tmp/pi-unit3-package-final.log`, SHA-256 `40ea8390a47cde64cf7fa4cb35db021791ddfc9c36b653b119a6b457725ba82b`. `git diff --check`: clean.

Changed behavior surfaces: lib/native-review-cli.ts, runtime/native-review-cli.mjs, lib/agents-runner.ts, lib/agents-protocol.ts, lib/agents-history.ts, extensions/gentle-agents.ts, extensions/gentle-ai.ts, lib/sdd-status.ts, lib/sdd-preflight.ts, assets/agents/sdd-remediate.md and scripts/verify-package-files.mjs. Paired tests: native-review-cli, sdd-selection-transport, agents-runner, gentle-agents, new sdd-managed-runtime-settlement, delegated-key-learnings-contract and package-manifest. Parent additionally authorized only tests/runtime-metrics-children.test.ts to assert remediation stays unsupported/dark, preserving every existing actor classification; no production telemetry change. Historical migration hashes/bytes were not edited. Tasks and this cumulative progress are the remaining artifact edits.

Rollback boundary: revert only this unit's paired source/mirror/asset/test hunks and task/progress completion update, preserving units 1–2, stale-artifact protections, the earlier workload-stop record, native history and index. No production rollback executed. Controlled fixture teardown and shell abort returned; process inspection found only the inspecting shell, no surviving test/harness/sleep process. No live provider/managed child, downloads, global installation, branch/commit/PR/release or delivery. Tests use fake managed children, actual isolated asset installation and real stock-shell execution; independent real producer → Pi → managed-child tuple and native record/charge inspection remain task 5.1/parent review, not claimed here.

One eventual size-exception Pi PR remains the delivery boundary; this cohesive unit is a verification/rollback boundary only. Pre-artifact delta measured 909 A+D; exact final Git A+D including this append/task changes is reported with actual settlement/readback in the phase envelope. Parent supplied a canonical native untracked inventory and explicit exact eight-path selection including both new files; use that ruling unchanged for settlement, without staging or inventing a digest. Passing settlement binds the final full-suite evidence above; its actual compact response is reported separately, never presumed in this pre-settle artifact.

### Remaining exact unchecked task lines
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Pi unit 3 — five frozen critical review corrections

Consumed authoritative OpenSpec `sdd-runtime-simplification` apply-ready v2 status, repo-local actionContext and the same canonical allowed workspace root. Proposal/specs/design/tasks/config and previous progress were read directly; prior historical bytes are preserved. The explicit cohesive size exception admits `pi-unit3-review-corrections`, evidence goal “Resolve five native unit-3 critical review findings”, one attempt / 900 A+D; forecast 620–885 A+D. Exact active continuation `pi-unit3-review-corrections-cont-a` returned proceed before correction work. Admission tree: `81f4076b44407162814a3cedf5a4fed3e2394180`. No executor reset, rescope, supersede, second attempt, staging or delivery. Skill resolution: paths-injected; CodeGraph timeout fallback was explicitly accepted; no actionContext warning.

Preservation matched the authorized baseline: original focused command 280 passed / 1 skipped; `pnpm test` 2198 passed / 10 skipped, plus provider contract and runtime harness, all exit 0. Before/after baseline Git status matched exactly. These are preservation results, not correction acceptance.

### TDD Cycle Evidence
| Frozen finding / boundary | Genuine RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| R1-remediation-scope-bypass | Missing explicit grant API; malformed child initialization left tools unrestricted | Canonical exact file intent intersects native roots; host UI displays worktree, roots and every exact command/cwd before acquire or persistence. Denial/cancel/headless yields zero acquire/persist. Symlink, directory, duplicate file and containment failures refuse. Child defaults to deny, enforces exact paths/commands/cwd/count, and grants never authorize another launch. |
| R3-acquire-durability / R4-acquire-durability-gap | No saved request before mutation; disk failure still allowed acquire | Existing task history retains request before native mutation, token before dispatch and actor-claim uncertainty before queueing. One exact ambiguous replay only; request/token persistence failures admit no actor. Real registered-host fixture inspects physical history before acquire/spawn. Reloaded unresolved request/actor history refuses another actor; pruning retains it. |
| R3-duplicate-command-evidence | One execution incorrectly satisfied four equal-text plan slots | Parent assigns indexed verification/harness/rollback slots to distinct call IDs; child executes each confirmed invocation only once. Reused IDs and exhausted invocation counts refuse; all four executions are required. |
| R4-research-write-crash-recovery | Desired bytes absent from pre-write history; corrupt snapshot accepted; in-memory session still allowed mutation | Bounded desired identity/scope precedes backend mutation; result facts and accepted identities persist in existing session history. Physical checkpoint tail must match. Fresh extension reload reads actual OpenSpec/Engram bytes and newer revision; missing results may converge only to exact desired identity. Wrong scope, stale bytes, malformed journals and partial hybrid updates block without store switching/replay. |
| Actor contracts / refactor | Missing one-launch authority and crash-readback instructions | Managed assets document scope, separate repeated executions and retained-operation refusal. Existing task construction was extracted rather than adding a ledger; canonical path logic is reused; focused/full suites rerun. |

Export-missing R1 is API RED, not behavioral proof. Later child-default-deny, duplicate-slot, pre-acquire persistence, research desired identity, malformed snapshot and in-memory-session failures are behavioral RED. Triangulation fixtures that passed first are coverage additions, not claimed defect RED. Fixture-only corrections for missing mock status/context and expected rejection semantics are not production failures. Logs: `/tmp/pi-corrections-{slots,scope,acquire,research,child-deny,assets}-red.log`, research triangulation/durability RED logs, corresponding GREEN logs and admission/reload triangulation logs.

Final verification commands, all exit 0 with no failures:
- Original focused file set plus `tests/sdd-research-capabilities.test.ts tests/sdd-research-live.test.ts`, using `node --experimental-strip-types --test`: 303 passed / 2 opt-in skips, `/tmp/pi-corrections-focused-final.log`, SHA-256 `2eaaa80b941c46c7a54df09938bea5f69aff1a4d2edf0a73617220b03f59bcc3`.
- `pnpm test`: 2211 passed / 10 skipped (exactly 13 added regressions), provider contract mirror and runtime harness pass; `/tmp/pi-corrections-suite-final.log`, SHA-256 `437008a603e2fd7f14d470d27a7e4b347800ece56c2a47f780f2c9100f1805cc`.
- `pnpm run check:runtime-modules`: six mirrors match without updates, `/tmp/pi-corrections-modules-final.log`.
- `node scripts/verify-package-files.mjs`: 169 files / 69 exact byte-pinned artifacts pass, `/tmp/pi-corrections-package-final.log`.
- `git diff --check` clean; before/after final verification status identical (`/tmp/pi-corrections-{before,after}-final-status.txt`). New mkdtemp fixtures use teardown removal. No live provider or managed actor was launched; ordinary mocked runner/stock-shell tests only.

Changed correction surfaces: `extensions/gentle-agents.ts`, `lib/agents-runner.ts`, `lib/agents-protocol.ts`, `lib/sdd-research-capabilities.ts`, `assets/agents/sdd-remediate.md`, `assets/agents/sdd-research.md`; paired tests `sdd-managed-runtime-settlement`, `gentle-agents`, `sdd-research-capabilities`; tasks and this append. Native adapter/mirror, telemetry, other prior source changes and historical artifacts remain unchanged. Pi's SDK can defer custom entries in unflushed/in-memory sessions: a void append is not durability, hence bounded physical readback. Exact command confirmation is not generic shell sandboxing or universal filesystem atomicity.

Task 4.3 was unchecked while corrections were incomplete and is now visibly checked after all gates passed; tasks readback is 12 complete / 2 pending. Independent review of these five findings remains parent-owned. Task 5.1 is still real producer → Pi → managed-child tuple re-proof, and 6.1 is final independent verification; neither is claimed complete. Rollback only these correction hunks with paired assets/tests, preserving prior units and native history. One eventual approved size-exception Pi PR remains the delivery boundary, not an action performed here.

This append precedes native terminal settlement so its bytes belong to the measured candidate. Exact final A+D, process cleanup and actual compact settlement/readback are retained in `/tmp/pi-corrections-incremental.txt`, `/tmp/pi-corrections-process-final.txt`, `/tmp/pi-corrections-settle.json` and the phase envelope; terminal success is not presumed by this pre-settle paragraph. No candidate edits are permitted after terminal settlement.

### Remaining exact unchecked task lines
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Final four-finding correction — review-e9d5805574817c23

Consumed authoritative apply-ready OpenSpec status and repo-local allowed root; native generation 10 admitted exactly `pi-unit3-final-review-corrections`, one attempt / 200 A+D, eight explicitly selected untracked paths. Previous complete generation 9 and missing-untracked-scope refusals were not bypassed. No reset, supersede, installation, binary switch or delivery. CodeGraph timed out; targeted reads used. Skill resolution: fallback-path (attempt-ledger skill/global strict-TDD guidance; phase skill unavailable at attempted installed path).

Smallest correction: deny remediation memory tools because no bounded human memory scope exists; separately scoped research tools remain unchanged. Shared history predicate permits ordinary blocked/complete acquisition records to age out and cease wedging launches, while preserving unknown acquire, tokens, actor claims and unresolved settlement. Hybrid second writes compare desired revision/digest before checkpoint/mutation; durable divergent identities fail closed on restart.

### TDD Cycle Evidence
| Finding | Safety net / RED | GREEN / triangulation / refactor |
|---|---|---|
| Memory scope | 117 focused passes; unauthorized memory assertion failed | All four memory tools denied; existing file/command and research memory positive cases pass |
| R3/R4 blocked acquire history | Reload and pruning assertions failed | Blocked and complete histories no longer wedge; uncertain acquire/actor histories remain retained/refused; shared predicate replaces duplicate logic |
| Hybrid divergence | Pre-mutation and restart assertions failed | Different revision and different bytes refused without checkpoint; matching identity admitted; crash and malformed restart controls pass |

Commands: `node --experimental-strip-types --test tests/sdd-managed-runtime-settlement.test.ts tests/gentle-agents.test.ts tests/sdd-research-capabilities.test.ts` — preservation 117 pass, RED 5 failures, final GREEN 117 pass. Logs `/tmp/pi-final-{preservation,red,green}.log`. `pnpm test` — 2211 pass/10 opt-in skips plus provider mirror/runtime harness, exit 0 (`/tmp/pi-final-suite.log`). `pnpm run check:runtime-modules` and `node scripts/verify-package-files.mjs` exit 0; `git diff --check` clean. No live provider; controlled tests only. No additional refactor needed beyond the shared history predicate.

Changed files: extensions/gentle-agents.ts, lib/agents-history.ts, lib/sdd-research-capabilities.ts, tests/gentle-agents.test.ts, tests/sdd-managed-runtime-settlement.test.ts, tests/sdd-research-capabilities.test.ts, this progress append. Task 4.3 temporarily unchecked and restored after all corrected gates passed; final task bytes unchanged, 12 checked/2 pending. Exact unchecked 5.1/6.1 lines remain verbatim in the preceding section. Independent review and final integration remain pending; no Ready-for-verify claim.

User reports Gentle AI's part merged into main: retained for 5.1/6.1, not independently verified and not installation authority. Rollback only these correction hunks, preserving prior candidate/history. One eventual approved size-exception PR, no delivery; current correction bound remains 200, not an expanded allowance. Final byte comparison and actual terminal settlement are reported in the phase envelope; this append precedes settlement to avoid post-terminal candidate drift.

## Installed managed uptake — pre-RED workload stop

Consumed injected authoritative OpenSpec apply-ready v2 status, repo-local actionContext and this exact allowed workspace. Read proposal/specs/design/tasks/config and cumulative progress; strict TDD guidance and fallback attempt-ledger/evidence skills loaded. Phase skill was unavailable at attempted installed paths; skill_resolution: fallback-path. CodeGraph inspected current installation and runner selection. Native fresh-CAS acquire admitted generation 11 `pi-installed-managed-uptake`, one attempt / 290 A+D, exact authorized eight-path selection and inventory. Admission tree: `b45d03190be6376f4fd676eb1f47f5849efd2261`. No reset/rescope or authority change.

Blocking decision: complete controlled no-provider proof forecasts 335–440 A+D: fixed SDK loader/observer and real runner process 150–190; native fixtures, installed identity/manifest, typed remediation and missing-asset controls 120–160; scope/stale/worktree/persistence controls and evidence 65–90. Existing real research boundary is live-auth dependent; substituting its fake-child or decoder seams would not meet 5.1. This is a forecast, not measured implementation. Parent queries timed out; maintainer was notified immediately on follow-up. Need an explicitly approved larger bounded successor or narrower accepted slice; no automatic budget increase or weakened acceptance.

Exact AI local main and Pi HEAD independently read: `d521b5e360ba441abaf894b64463fd53805cbb16` and `6c4a69310c1e19c4a11a94e2f58f9706b73fbde9`. `git -C /home/gentleman/work/gentle-ai archive d521b5e360ba441abaf894b64463fd53805cbb16 | tar -x -C /tmp/pi-installed-uptake.1W1EvwBd/ai-main` materialized only that identity, not the ambient branch. In the archive, `go build -o ../gentle-ai ./cmd/gentle-ai` exited 0; Go 1.26.5, binary SHA256 `56ce756a0cd7bac73ff0080bf2ff42d3c2d0f31b17c4d272750f4bfc28b06326`. Archive has no VCS build stamp; recorded source ref and invocation bind provenance. No worktree was registered. Pi package was not built; no installed tuple or managed-child proof claimed.

| Task | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| 5.1 | 123 focused pass/1 skip; producer-backed 20 pass; dev-binary 7 pass/3 skips | Not authored: workload gate | Not reached | Not reached | Not reached |

Commands from this Pi checkout: `node --experimental-strip-types --test tests/sdd-managed-runtime-settlement.test.ts tests/sdd-selection-transport.test.ts tests/gentle-agents.test.ts` exited 0 (123 pass/1 producer opt-in skip). With `SDD_TEST_PRODUCER=/tmp/pi-installed-uptake.1W1EvwBd/gentle-ai`, `node --experimental-strip-types --test tests/sdd-selection-transport.test.ts` exited 0 (20 pass). With `GENTLE_AI_DEV_BINARY` set to that binary, `pnpm run test:dev-binary` exited 0 (7 pass/3 host-relay skips; not full dev-binary coverage). `pnpm test`, runtime-module/package gates and the new boundary were not run after the workload stop. No behavioral RED or completion inferred from preservation.

Evidence retained mechanically under `/tmp/pi-uptake-evidence/`: preservation.log, producer-preservation.log, dev-binary.log, ai-build.log, ai-source-identity, ai-binary.sha256, ai-build-info.txt and ai-worktrees.txt. Isolated archive/binary directory removed; no worktree-registration cleanup needed. Foreground commands exited; no provider or managed child launched by this work. No global install, asset install, source/test edit, index mutation, commit/delivery/release. Only this append changes the candidate; rollback removes it alone, preserving all prior bytes and immutable native history. Final exact A+D, process check and actual interrupted settlement are reported separately; this append precedes settlement, with no post-terminal candidate edits.

No task completed: tasks readback remains 12 checked/2 pending, including exact unchanged 5.1/6.1 unchecked lines in the preceding Remaining section. No persisted checkbox update was appropriate. One eventual approved size-exception Pi PR remains only a delivery boundary; it does not enlarge this native cap. Independent verification/acceptance and complete installed tuple remain pending.

## Expanded installed uptake — behavioral RED retained; edit-surface blocker

Authoritative OpenSpec apply-ready v2 status and repo-local allowed roots were consumed; all earlier local input artifacts/config/progress remain the phase basis. Native status supplied `sha256:bdbbc4091c6086f0283b58c0734127279fec2c73043b11fac1cafdb7b9a67dec`, not the stale user-supplied revision. Explicitly authorized supersede used that fresh revision, exact actor `maintainer`, exact supplied reason, `pi-installed-managed-uptake-expanded`, one attempt /440 maximum; resulting generation12 revision `sha256:efcc459c314bd2ea38252b4d2e43eb05a29ddcf52d7352f3b532aa085156622d`. Fresh acquire proceeded with the exact existing eight-path selection/inventory. Admission tree `7afee9e495c04879e892c5c17e379cc58322750f`; prior18 lines remain cumulatively charged. No additional maintenance operation. Skill resolution fallback-path; installed SDK/extension docs read completely, CodeGraph context reused, later reads targeted only to failing boundaries.

Added `tests/sdd-native-managed-uptake.test.ts`: real SDK ResourceLoader with fixed extension paths, no ambient discovery, finite local scripted model (no provider network), real RPC child and AgentRunner; parent inspects model input, RPC tool results, actual filesystem effects and installed manifest/content. Isolated current Pi copy runs runtime-module generation; apply/research/remediate assets install only to temporary home. Independently rebuilt AI from exact `git archive d521b5e360ba441abaf894b64463fd53805cbb16`, then `go build -o ../gentle-ai ./cmd/gentle-ai`; ambient AI branch never built. AI binary SHA256 `ef62f3f3f8b01d202a2e4c7bcd66eec48bee684401d33c82fda0f789b7ee075f`; Go1.26.5. Pi HEAD `6c4a69310c1e19c4a11a94e2f58f9706b73fbde9`; current source-content manifest SHA256 `3e52eba9f1466a7e07ad5b6c227ea3514e7d13b9a8eca6fec7152eafee5eb844`. This is partial build/probe evidence, not an accepted complete tuple.

| Task/boundary | Safety net / RED | GREEN / triangulation / refactor |
|---|---|---|
| 5.1 installed tuple |123 focused pass/1 skip. Initial controlled missing-install ENOENT is deployment-fixture RED, not production defect. Fixture import/flag collision corrections are harness errors, not behavioral RED.|Real native apply input and read pass; installed three-actor manifest/content equality passes; research present/missing capability, actual exact read/write/readback and out-of-scope refusal pass.|
| Wrong worktree / actor routing | Genuine real-child RED: blocked native selection still executed read; installed remediation body did not receive native status.|Allowed `extensions/gentle-ai.ts` now mechanically blocks tool calls on startup selection errors (parent messaging retained), and reuses typed phase detection to recognize remediation. Wrong-worktree negative passes; remediation native prompt now matches.|
| Remediation scope | Real child read remains denied despite matching selected status and supplied narrow plan/scope.|Not GREEN: SDK `getFlag()` only reads flags registered by that same extension (`dist/core/extensions/loader.js:287–292`). `gentle-agents.ts:553` reads `gentle-sdd-change`, owned by gentle-ai, so scope initialization silently fails. Needs narrow existing-payload selection transport into `extensions/gentle-agents.ts`, which is OUTSIDE allowed edit surfaces. No workaround/SDK patch applied. Stale-remediation follow-up assertion was not reached.|

Concrete blocker was reported immediately twice and queried to parent (timeout): authorize `extensions/gentle-agents.ts` narrowly to consume runner-carried selection from existing remediation transport, with corresponding allowed runner/test changes. Do not broaden permissions or claim a complete tuple. Only extension/test/progress changed; task5.1 and6.1 remain unchecked,12 checked/2 pending; no completion checkbox update is appropriate. Full read-only/excluded-marker installed-boundary re-proof and final stale control remain incomplete. Existing preservation covers host scope, not new full tuple acceptance.

Commands: focused `SDD_TEST_PRODUCER=/tmp/pi-uptake-expanded-build/gentle-ai node --experimental-strip-types --test tests/sdd-native-managed-uptake.test.ts` final exit1,5 passing subtests and failing remediation subtest plus parent failure. Genuine RED log `pi-uptake-expanded-boundary-red.log`; current focused log `pi-uptake-expanded-boundary-green.log` is still FAILED despite filename. `SDD_TEST_PRODUCER=... pnpm test` exit1:2217 pass/2 failures/9 skips; both failures are the new remediation subtest and parent. Provider/runtime-harness suffix did not run after unit failure. `pnpm run check:runtime-modules` exit0 (six mirrors); `node scripts/verify-package-files.mjs` exit0 (169 files/69 pins); `git diff --check` exit0. Full gate is not green. Dev-binary not rerun in this expanded attempt; earlier7pass/3skip remains only prior preservation.

Evidence copied mechanically into `/tmp/pi-uptake-expanded-evidence/`, including logs, AI build info/hash and Pi content manifest. Every test fixture/package/home/session is removed by teardown; standalone AI archive/binary removed, no worktree registered. No live providers, global install, dependency changes, unrelated edits, commit/delivery/release. Runtime-module generation occurred only in temporary Pi copies. No successful remediation settlement or native-inner correction bracket claimed: probe only tests controlled input/read boundary against a declared failing verification fixture. Outer attempt will settle interrupted on the edit-surface blocker; actual native response, A+D and any untracked-selection refusal are reported separately. No passing evidence or Ready-for-verify claim. Rollback only this attempt's two source detection/guard hunks, new test and this append; retain all prior history and evidence.

## Ordinal 13 continuation — task 5.1 implemented

Maintainer explicitly added only `extensions/gentle-agents.ts` for selection transport and the ninth intended untracked test with native inventory `sha256:4384df0d85fe8735436832d3e4858c972d3067df39073e7079977f23429cf8e8`. Exact active-token acquire returned proceed: same ordinal13/generation12, no new attempt/reset/supersede and no budget increase. Earlier blocked settlement remains history, not terminal closure. The authoritative apply-ready OpenSpec/repo-local actionContext is unchanged; authorized roots still this worktree only. Existing inputs and strict-TDD guidance were consumed earlier in this cumulative run; skill_resolution remains fallback-path.

Completed 5.1 and checked its persisted checkbox immediately after final boundary GREEN. Tasks now13 checked/1 pending;6.1 remains independent acceptance, not implemented by this executor. Minimal source changes: typed phase detection recognizes installed unnamed remediation; explicit native startup selection failures now block actual tools while preserving parent notification; runner carries the same selection in the existing remediation-plan payload, and the child validates it instead of depending on another extension's owner-local flag. The existing flag accessor remains only when no transported selection exists; an explicitly malformed transported value is not silently replaced. No new protocol, ledger, dependency or permissions mechanism. Existing shipped actor instructions already require these refusals and are unchanged.

### TDD Cycle Evidence
| Boundary | RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| Actual native selection / installed actor | Prior real child read succeeded after wrong-worktree refusal; installed unnamed remediation missed native prompt. |Both defects fixed. Positive native apply/remediation input matches producer JSON and installed actor body. Wrong-worktree/stale-remediation tool calls fail; ordinary positive read still executes.|
| SDK selection transport | Earlier full-suite remediation subtest and parent failed; SDK flag-ownership rule and absent child-local flag mechanically inspected. |Same real SDK/RPC test passes after same-payload transport. Existing focused regression fixtures remain green; no SDK patch or authority substitution.|
| Full installed tuple | Initial omitted installation was deployment-fixture RED, not production defect; import/duplicate-flag/plan-shape failures were harness corrections only. |10 boundary tests pass: native contract/v2, fixed extension argv and actual provenance, three installed manifest/content identities, missing asset before acquire, absent tool before capability execution, bounded denial→corrected same-path persistence/readback, forbidden write, stale/wrong-worktree research, typed remediation and native stale refusal. Installed host handlers with real native adapter and controlled UI answers prove read-only/excluded-marker suppression. Shared run/observer and deterministic build-inventory ordering refactor retain GREEN.|

Final commands from this worktree: `SDD_TEST_PRODUCER=/tmp/pi-uptake-final-build/gentle-ai UPTAKE_EVIDENCE_DIR=/tmp/pi-uptake-final-evidence node --experimental-strip-types --test tests/sdd-native-managed-uptake.test.ts tests/sdd-selection-transport.test.ts tests/sdd-managed-runtime-settlement.test.ts tests/gentle-agents.test.ts tests/agents-runner.test.ts` exit0,186/186. `SDD_TEST_PRODUCER=/tmp/pi-uptake-final-build/gentle-ai pnpm test` exit0,2222 pass/9 skips plus provider-contract check and runtime harness. Suite SHA256 `80872d86d8a0ed0bb6b4146e80f2b5484dcec307edf6ac88f0b385b3c1294759`. `pnpm run check:runtime-modules` exit0,six mirrors; `node scripts/verify-package-files.mjs` exit0,169 files/69 pins; `git diff --check` clean. No generated worktree bytes changed.

Dev-binary limitation, not hidden skip: with BOTH `GENTLE_AI_DEV_BINARY` and `GENTLE_PI_GENTLE_AI_DEV_BINARY` set to the independently built AI, `pnpm run test:dev-binary` runs10 cases and exits1:8 pass,2 reviewer frozen-manifest assertions fail. Both exact assertions reproduce on unchanged admission tree `7afee9e495c04879e892c5c17e379cc58322750f`, materialized by git archive in an isolated directory, with the same binary and direct package-declared Node command after temporary `git init`. Baseline pnpm initially refused its automatic dependency check before tests; no dependency installation was authorized or completed, so the identical declared Node test command was used. First direct baseline lacked Git metadata; final isolated-Git baseline reproduces both actual assertions. These review-lane defects predate this implementation and remain outside scope; full final acceptance belongs to6.1, not an all-gates-green claim.

Exact final tuple: AI archive source `d521b5e360ba441abaf894b64463fd53805cbb16`, built with `go build -o ../gentle-ai ./cmd/gentle-ai`; binary SHA256 `b94af4e5c019eebe50f0fbdfd29e9d300f4602ea523dccec42b3ca92926ea1df`. Test verifies this against the independent build sidecar, not an inferred version. Pi HEAD `6c4a69310c1e19c4a11a94e2f58f9706b73fbde9` plus current source; isolated Pi runtime generation and ordered content manifest yield build SHA256 `bb33da53f79f44ddb903abd27d94a5ddff3e3fea76819c45207103d891500bd6`. Installed apply/research/remediate SHA256 respectively `cfda30cec17858313f1a2dd57f5e92e715eb68f4a192ed3643df27dc9825d669`, `8f4dc20c533412ce3d1698c68ead1d38ec3e8c13f795345de9a8a959cd0b903b`, `04ad5288216d1459cb6decf77ffb7ef668bde1b5e543e76b3dfc841c8dc759b6`; contract floor `gentle-ai.sdd-status/v2`. Source assets and installed bytes compare exactly.

Evidence retained under `/tmp/pi-uptake-final-evidence/`: tuple.json (complete build and installed manifest), installed-agents/, per-case RPC events/argv/model inputs/physical sessions, producer-build.json, focused/full/runtime/package/dev-binary and baseline logs. Fixed SDK loader uses isolated HOME, no discovered extensions/resources, in-memory credentials and PI_OFFLINE; only a finite local scripted model emits predetermined calls. Parent assertions inspect actual inputs/results/files, never child verdict prose. Research tools are controlled extension fixtures, not live web evidence. This is boundary acceptance, not an actual source-correction lifecycle or independent final verifier. User-authorized AI archive/binary, baseline copy and all test homes/copies are removed after evidence retention; no worktree registration, live providers, global installation, commit/delivery/release.

Rollback only ordinal13 paired source/test changes and its task/progress completion update; preserve prior candidate/native history and earlier interrupted18-line evidence. Final exact A+D and actual terminal native response follow in the phase envelope; this append precedes settlement, with no post-terminal edits. Eventual single size-exception Pi PR remains a delivery boundary only. Next: parent-owned independent review/final task6.1, retaining the two baseline dev-binary failures as explicit risks.

### Remaining exact unchecked task line
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## Compatibility recovery — pi-review-escalation-status-compat

Consumed injected authoritative OpenSpec/apply-ready status, repo-local workspace and allowed root; proposal/specs/design/tasks/config and cumulative progress read directly. Exact user-authorized decoder recovery, one attempt/160 A+D, fresh CAS and exact nine-path inventory acquired proceed; no maintenance decision/reset required. Skill resolution fallback-path (phase skill unavailable; attempt/evidence/TypeScript skills and strict-TDD guidance loaded). CodeGraph timed out; targeted reads used. No actionContext warning; eventual PR topology unchanged, no delivery.

Producer ground truth read from AI commit d521b5e360ba441abaf894b64463fd53805cbb16: target_status.go, compact_reviewer_capture.go and last-event-closure.schema.json escalation definition. Minimal strict v7-only optional metadata typing/decoding preserves prior absence behavior, native action/transition/authority and older identities. No authority reconstruction, routing change or new conditional requirement for older v7 producers. Unknown keys, malformed arrays/strings, unsupported causes/outcomes and explicit null/undefined refuse.

### TDD Cycle Evidence
| Boundary | Safety net / RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| Optional v7 escalation | 75 focused passes; producer-shaped escalated fixture genuinely failed with status.escalation is not allowed | 77 passes; six causes, three refuter outcomes, absent evidence, empty/nonempty arrays, malformed/unknown keys and older-version refusal; same-fixture comparison preserves all routing/authority fields. Comment-placement refactor and mirror regeneration rerun green. |

Commands: node --experimental-strip-types --test tests/review-integration-v2.test.ts tests/review-integration-v2-forward.test.ts; pnpm test (2213 pass/11 opt-in skips, provider check/runtime harness pass); pnpm run check:runtime-modules (six match); node scripts/verify-package-files.mjs; git diff --check. All final exits zero. Logs /tmp/pi-escalation-{preservation,red,green,focused,suite,modules,package}.log. Final suite SHA256 992a4bd04010f6e0952f5cad69eba2af0458654f76f128c08964e3b5770dcf9e. No live-provider or installed tuple rerun; baseline dev-binary limitations above remain.

Changed only lib/review-integration-v2.ts, generated runtime/review-integration-v2.mjs, tests/review-integration-v2-forward.test.ts and this append. Rollback only these paired hunks, preserving prior candidate/history. Generated activation artifact SHA256 cd123aa236a91d6750b3a6cbb27999da5609f948d748b70c0b003ee0e69861e0; parent owns any activation/reload. No review STATUS/capture/recover, activation, global install, live provider, commit or delivery performed. Foreground checks exited; no new persistent test fixture.

No planned task completed by this unplanned authorized compatibility recovery: tasks readback remains13 checked/1 pending; task6.1 exact unchecked line remains verbatim in the preceding section. No checkbox modification permitted/needed; independent final verification remains parent-owned. This append precedes terminal settlement; actual compact response and exact total A+D reported in phase envelope, with no post-terminal candidate edits.

## Native review correction — pi-known-acquire-failure-retry

Consumed the authoritative OpenSpec apply-ready status and repo-local allowed root. User-authorized acquire `pi-known-acquire-failure-retry` proceeded with token `sha256:08caab401ccba1f66510990cd2c47cb48e5b972a77e56f0682e52367251a1d71`, one attempt/100 A+D, exact nine-path untracked selection and inventory `sha256:4384df0d85fe8735436832d3e4858c972d3067df39073e7079977f23429cf8e8`; no maintenance decision, reset, rescope, review status/capture/validation, activation, installation or delivery occurred. User-directed targeted reads replaced the previously timed-out CodeGraph query. Skill resolution: fallback-path (phase skill path was not injected).

For `TypeError` or `NativeReviewCliError` with `mutationOutcome === "none"`, acquire now durably records `{ state: "blocked" }` before rethrowing. That terminal result no longer triggers `remediationUnresolved`, so a later managed launch may retry. Ambiguous errors still get one identical replay and persist `acquireUncertain`, which remains fail-closed. Changed only `extensions/gentle-agents.ts` and `tests/sdd-managed-runtime-settlement.test.ts` within the authorized surfaces; no runtime mirror changed.

### TDD Cycle Evidence
| Boundary | Safety net / RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| R3/R4 known acquire failure | 104 focused pass; RED 34 pass/1 fail: missing terminal `acquireResult` | 104 focused pass; TypeError and real `NativeReviewCliError` cover two known-no-mutation paths, while ambiguous replay remains unresolved. No refactor beyond the minimal branch. |

Commands all exited 0 after GREEN: `node --experimental-strip-types --test tests/sdd-managed-runtime-settlement.test.ts tests/gentle-agents.test.ts` (104 pass), `pnpm test` (2213 pass/11 skipped plus provider/runtime harness), `pnpm run check:runtime-modules`, `node scripts/verify-package-files.mjs`, and `git diff --check`. Logs: `/tmp/pi-known-acquire-{preservation,red,green,suite,runtime,package}.log`; suite SHA-256 `21ebb59d5660dd27bf6549e4ef235ac2de45fbf9ef27e4937ba0f6a894df8508`. No surviving test process was found.

A+D against frozen correction tree `58cd4b98064efda27fc47b0bc78247c67e26d111`, using direct frozen-file comparison: `extensions/gentle-agents.ts` 5/1 and `tests/sdd-managed-runtime-settlement.test.ts` 11/1, **18 total**—within the 80-line correction plan and 100-line attempt cap. No planned task completed; persisted tasks remain 13 checked/1 pending and task 6.1 remains unchecked. Rollback only this source/test branch and paired regression; preserve all prior candidate/history. This append precedes terminal settlement; do not edit the candidate afterward.

## Native status guidance cutover — pi-native-status-guidance-cutover

Consumed the authoritative OpenSpec apply-ready status: repo-local workspace and allowed root match this worktree, with 13 checked tasks and task 6.1 still pending. Fresh acquire returned `proceed` for one attempt/550 A+D with the exact nine-path untracked selection and inventory `sha256:4384df0d85fe8735436832d3e4858c972d3067df39073e7079977f23429cf8e8`; no maintenance decision, reset, rescope, source routing, installer pin, installation, delivery, or AI checkout/ref mutation occurred. CodeGraph exploration timed out, so targeted reads were the documented fallback. Producer provenance was verified from Gentle AI `01e6fac15df169e8993e02549b9edcbd01abc0ee`, tree `f216ba572263d350c3ece86e72e12be38eb70c76`, merged PR #4505, using an isolated `git archive` build (binary SHA-256 `d58396675e4aba6a04ea5d955939a263fbe68f05f15a6b1c73dc5965a510bda7`).

### TDD Cycle Evidence
| Boundary | Safety net / RED | GREEN / TRIANGULATE / REFACTOR |
|---|---|---|
| Producer provenance and five guidance surfaces | Initial producer-focused run: 5 pass/1 stale d521 provenance failure. Test-first RED then failed on stale local authority, status reconstruction, and bypass guidance. | One-line producer pin now asserts 01e6; all five surfaces require native v2/read-only status and reject local reconstruction/bypass. Planning/execution and artifact-language contract tests preserve native-only routing and manual sdd-sync. |

Final evidence: producer-backed focused command passed 45/45; `SDD_TEST_PRODUCER=/tmp/pi-native-status-guidance-01e6/gentle-ai pnpm test` passed 2223/2223 with 9 opt-in skips, provider contract, and runtime harness. `pnpm run check:runtime-modules`, `node scripts/verify-package-files.mjs`, and `git diff --check` passed. `pnpm run test:dev-binary` with the isolated 01e6 binary had the established unrelated baseline: 8 pass/2 fail in `tests/devbinary/pi-host-relay.devtest.ts` frozen-manifest assertions (lines 381 and 661); no touched source/test path participates, so it is retained as a baseline risk, not repaired here.

Changed only the five authorized guidance surfaces and paired existing guidance/routing/provenance tests: `assets/support/sdd-status-contract.md`, `assets/sdd-orchestrator-workflow.md`, `assets/agents/sdd-status.md`, `assets/agents/sdd-verify.md`, `assets/agents/sdd-archive.md`, `tests/sdd-native-managed-uptake.test.ts`, `tests/sdd-execution-routing-contract.test.ts`, `tests/sdd-planning-routing-contract.test.ts`, and `tests/artifact-language.test.ts`, plus this cumulative evidence append. No task was completed by this correction and no checkbox changed; tasks remain 13 checked/1 pending, with 6.1 the exact remaining independent final-verification task. Manual sdd-sync and its local resolver are explicitly preserved. Rollback removes only these guidance/test/evidence hunks; do not edit after terminal settlement.

## 6.1 independent final verification — recorded

The independent verifier returned **PASS**; task 6.1 is checked. Authoritative AI provenance is `01e6fac15df169e8993e02549b9edcbd01abc0ee` / tree `f216ba572263d350c3ece86e72e12be38eb70c76`; AI full suite and vet pass. Producer-bound Pi full suite passed 2223/2223 with 9 skips; installed boundary passed 10/10; runtime modules (6), package verification (169 files + 69 pinned artifacts), and diff checks passed. The dev-binary result remains the exact admission-tree baseline, **8 pass / 2 fail**, and is not green. Guidance correction is authoritative at 356/550 A+D despite writer prose reporting 355.

Final pre-record candidate tree: `97a1d60e6ac98fae973b4fc2754c5b4d6682482e`. Before this record, the ledger reported ordinal 16 complete with no active attempt. Every spec ID is PASS. This review-only record has no TDD production-code cycle; it records the independent evidence. A fresh candidate-bound review is required before any delivery; no delivery is authorized or performed.
