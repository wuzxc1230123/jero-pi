# Proposal: simplify SDD runtime interpretation — Pi adoption

## Current Pi ownership and authorization

This self-contained plan adopts the approved cross-repository proposal without redesign. AI's three accepted units are external prerequisites, not work performed here or reusable Pi authority. Pi owns four remaining units: tasks 2.1–2.3; the Pi portion of 3.1 plus 3.2; 4.3; and 5.1. Task 6.1 is final cross-repository verification, not another implementation unit. The canonical state is six checked and eight pending; Pi execution has not begun.

Retain automatic mode, OpenSpec artifacts, ask-on-risk, and seven internal units total. The 400 authored additions plus deletions target permits justified cohesive overruns, never increased native attempt budgets. Exactly one eventual Pi PR uses `size:exception` (`exception-ok`, `size-exception`), not a chain or one PR per unit. Full planning documents count in that eventual diff. This current policy supersedes historical hard-400, integrator-only exception, branch-chain and pre-implementation design-review stops.

Only this planning directory is authorized now. No source implementation, attempt acquire/settle, branch, commit, PR, installation, live-provider/managed-child execution or delivery is authorized. Future execution requires fresh native status and correctly scoped native `proceed`; do not reopen/reset the completed AI objective or reuse external AI authority.

## Decision and intent

For gentle-ai#4484, remove contradictory refusals through one native-selected action; Pi never chooses transitions. The approved decision basis was admission r8 and its evidence/research conclusions. Research/write/permanent-block reports and #4470 remain symptoms and acceptance targets, not reproduced causes or fresh Pi verification.

## Scope: smallest systemic correction

**Immediate mismatches**

- Align Pi with AI's seven dependencies/four instruction groups; delete exact-three assumptions/prose routing.
- Remove unrelated launch/review/delivery gates from read-only status and contradictory Engram dispatch bans.
- Align research declarations, actual tool names/grants/provisioning; carry backend/path and already-authorized OpenSpec writes across phases.

**Necessary completion**

- Re-evaluate corrected facts through existing native recovery predicates, not stale blockers.
- Complete bounded typed remediation: failed-evidence revision binding, Pi execution, native settlement, fresh independent verification. Never alias apply.
- Reuse subject/evidence/accounting. Add only proven missing facts; name the failing acceptance scenario and why consolidation/plumbing cannot suffice.

## Retained guarantees; non-goals

Keep immutable common-directory ledger/history, CAS, candidate/subject/worktree/consent bindings, one active attempt, finite truthful outcomes and capped refunds. No-diff execution is not free. Keep strict RED/GREEN/TRIANGULATE/REFACTOR, required implementer–verifier independence, durable planning and advisory-only RDD.

No new engine, ledger, DSL, generator, framework, compatibility layer, waiver, automatic reset, redundant verification gate, copied/ported/extracted source or upstream dependency.

## Capabilities and affected areas

- **New:** [sdd-native-runtime](specs/sdd-native-runtime/spec.md): existing status/action/recovery/settlement/consumer behavior, not new machinery.
- **Modified:** [sdd-orchestrator-assets](specs/sdd-orchestrator-assets/spec.md): native interpretation/authorized handoff; [sdd-research](specs/sdd-research/spec.md): truthful grants/recovery.
- **AI:** `internal/cli/sdd_status.go`, `internal/sddstatus/`, `internal/assets/`, `internal/components/sdd/` in the external AI repository.
- **Pi:** `lib/native-review-cli.ts`; managed SDD executor/provisioning/assets in this repository.

## Acceptance and measurement

Future scenarios, not results:

1. OpenSpec/Engram status needs no launch authorization; writes no authority.
2. Producer fixtures decode consistently; malformed/unknown actions refuse.
3. Actual research grants → authorized writes → corrected-fact continuation.
4. Missing tools, out-of-scope writes, stale evidence and wrong worktrees still refuse.
5. Bound remediation reaches independent verification; unsupported consumers refuse without invented commands.
6. #4470 historical replay reconciles; fresh selections/drift remain strict.
7. Failed/interrupted/no-diff outcomes settle finitely; lost acknowledgements reconcile without re-execution/double accounting; unknown effects stay unknown.
8. Managed-child execution proves uptake/strict quality; self-report/process-success never replaces verification.

Measure removed duplicate decisions and scenario coverage. [Design](design.md) contains the complete contract, proof matrix, limitations and rollback boundaries; [tasks](tasks.md) contains task details, commands and exact state.

## Risks, dependencies and rollback

Pi #593/#605/#608 uptake remains unproven; assume no merges. Design sets schema/compatibility floor; fix v2 where sufficient. Rollback producer/consumer/assets together; preserve history/budgets/consent. Refuse execution if rollback cannot safely read retained records.

## Minimal sequence

Specify/design boundaries → plan immediate fixes, necessary completion/uptake → tasks → correctly scoped native admission → per-unit strict TDD and independent verification before advancement. This adoption is planning only, not execution or delivery authorization.

## Non-normative provenance

These historical locations are optional provenance only; no approved requirement or current authorization depends on their continued availability:

- Canonical planning source: `/home/gentleman/work/gentle-ai-worktrees/sdd-runtime-simplification-plan/openspec/changes/sdd-runtime-simplification/` (proposal, design, tasks, preproposal admission r8, research and evidence).
- External acceptance handoff: `/tmp/sdd4484-pi-next.i70lrxm0/handoff.md`.
- Tracker: https://github.com/Gentleman-Programming/gentle-ai/issues/4484.

External artifacts are unchanged. Historical research, pending notes and temporary evidence are not fresh verification or current Pi status.
