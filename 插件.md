# 1. 核心上下文、代码库感知与长记忆
pi install npm:pi-fovea               # 全局依赖图谱与 Token 预算控流
billion-context-pi
pi install npm:pi-memory                 # 跨 Session 架构决策持久化e

# 2. 精准编辑与多维代码自纠错（注意：用 pi-lens 替代单独的 pi-lsp）
pi install npm:pi-hashline-edit-pro    # 基于 Hash 锚点的绝对防错行编辑
pi install npm:pi-lens                 # 多维代码检查（内置 LSP + Linter + Formatter + Ast-Grep）

# 3. 任务拆解与多 Agent 交互
pi install npm:rpiv-todo                 # 内存存活任务链
pi install npm:rpiv-ask-user-question    # 歧义卡点向用户发问
pi install npm:pi-intercom              # 子 Agent / 跨 Session 通信

# 4. 基础设施与安全
pi install npm:pi-web-access             # 联网搜索与 Fetch
pi install npm:pi-git-agent              # Git 自动化
pi install npm:security-guidance         # 安全准则与规范

pi-cache-optimizer