# Review Ledger — orchestrator-lazy-diet

Round 1 — judgment-day (jd-judge-a + jd-judge-b, blind parallel) over the DESIGN artifacts. Pre-implementation. Verified sound by both: getOrchestratorPrompt cache/substitution description, :123-133 citation, 22,626 B total, no lazy-file double-delivery via the cache.

Round 3 — surgical fix pass. Rounds 1-2 had marked JD-002/JD-003/JD-005/JD-008/JD-010 "fixed" on the strength of stated intent and byte *estimates*, but no literal condensed core prose had actually been drafted — the Core Budget table held numbers with no text behind them. This round writes the actual prose for every "Split" core row into a new `design.md` "## Appendix: drafted core texts (measured)" section, measures each block with `wc -c`, and reconciles the Core Budget table, the Inventory table's `core ~N` annotations, and the Rationale paragraph against the measured figures (honest total: 9,945 B, 295 B under the revised ≤10,240 B threshold). It also corrects the JD-003 wrapper-growth figure against persona-single-channel's own converged numbers, removes a reintroduced comparator regression (JD-010), names the exact JD-005 test seam mechanism, and re-anchors every row below from line numbers (which drift on every insertion) to section/symbol names.

**File:line anchor policy (this round)**: all `file:line` cells below are now SECTION-NAME anchors (design.md heading text, or the referenced file's function/section name) instead of line numbers. Line-number anchors had drifted on 9 of 10 rows after this round's insertions; section-name anchors are insertion-order-independent.

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| JD-001 | judgment-day | CRITICAL | design.md § Inventory (measured, wc -c) | verified | Inventory table replaced with the judges' `wc -c`-verified figures (self-recomputed with `awk 'NR>=a&&NR<=b'\|wc -c` per section, reconciles to 22,626 exact), labeled "(measured, wc -c)". Round 3: fixed a residual row-label typo in the same table — 4R Review Triggers line range "298-313" corrected to "298-312" (`assets/orchestrator.md` has 312 lines total). |
| JD-002 | judgment-day | CRITICAL | design.md § Core budget rebuilt from measured drafts (Decision) / § Appendix: drafted core texts (measured) | verified | Round 3: the condensed core rows are now LITERALLY drafted in a new "## Appendix: drafted core texts (measured)" section (previous rounds claimed "drafted and measured" without writing the prose — this was the round-2 blocker). Every Split row measured with `wc -c`: Delegation Rules (1,468 B), Memory Contract (951 B), 4R (825 B), Work Routing Ladder (1,196 B), Skill Registry (720 B), Intent-Driven Skill Discovery (350 B), Language Boundary (1,393 B), Identity Contract (148 B). Honest total = 9,945 B (13-row subtotal 9,372 + 573 reserved), still under the revised ≤10,240 B threshold with 295 B headroom (down from the previous round's unverified 414 B headroom claim, now an actually-measured number). Individual-row overages vs. the prior estimates are itemized and reconciled in a new "Honesty note" paragraph. |
| JD-003 | judgment-day | CRITICAL (coordination) | design.md § Core Budget table (Identity Contract, Language Boundary rows) (vs persona-single-channel/design.md § Byte estimates, § Sequencing contract) | verified | Identity (148 B) and Language LB1 pointer reuse persona-single-channel's delivered pointer text VERBATIM, near-zero core cost. Decision rationale corrected: the budget benefit comes from core OMITTING identity/persona/language bytes entirely (pointing to the wrapper instead), not from a wrapper shrink. Round 3: replaced the imprecise "wrapper actually grows +0.4KB per persona's design" with the exact figure — persona-single-channel's own review-ledger.md Round 2 (scoped re-judge, terminal state JUDGMENT APPROVED) converged on wrapper Identity contract block 438 B → 817 B, delta **+379 B**, after that round's JD-002 reopened and corrected an inconsistent intermediate figure (861B/+520B) the fix had introduced. +379 B is now cited as persona-single-channel's final, judge-converged number (not a pending/round-3 range), since that change's own ledger already reached terminal APPROVED state on it. |
| JD-004 | judgment-day | CRITICAL (coordination) | design.md § Reserved: incoming Review Execution Contract (vs port-review-ledger-contract/design.md § Interfaces / Contracts, Ledger persistence branches) | verified | Added "Reserved: incoming Review Execution Contract" inventory row with a bounded estimate (from that change's § Interfaces / Contracts persistence-branches prose) included in the Core Budget total, plus a hard commitment to re-verify the budget with `wc -c` against the rebased file before the fixture freeze. Round 3: the bounded ~750-900 B estimate is now superseded by an actually-drafted representative rendering in the Appendix, measured at 573 B — the finding itself (reserve a row, commit to re-verification) is unchanged, but the number backing it is now measured text instead of an estimate. |
| JD-005 | judgment-day | WARNING | design.md § Testing Strategy (Byte budget row) / § Interfaces / Contracts (Test seam) (vs extensions/gentle-ai.ts § getOrchestratorPrompt, § ASSETS_DIR, § __testing) | verified | Testing Strategy byte-budget row targets `getOrchestratorPrompt()`'s RETURN value, not the raw file with unresolved placeholders. Round 3: named the exact test seam mechanism (previously only said "stub `ASSETS_DIR`/cwd" without saying how) — `getOrchestratorPrompt` is added to the existing `__testing` export object (`extensions/gentle-ai.ts` § __testing), and `ASSETS_DIR` gains a `GENTLE_PI_TEST_ASSETS_DIR` environment-variable override read once at its definition site, with an explicit note that the module-level `orchestratorPromptCache` is first-read-wins so the env var must be set before the first call in a test process. |
| JD-006 | judgment-day | WARNING | design.md § File Changes (tests/gentle-ai.test.ts row) / § Testing Strategy (Regression row) (vs tests/gentle-ai.test.ts § review-lens union assertion) | verified | Testing Strategy regression row states explicitly that `tests/gentle-ai.test.ts`'s review-lens assertion is a 3-file uniform loop and only the `orchestrator.md` iteration is repointed to the core+delegation-ref union; README/SKILL.md iterations are unchanged. Not touched this round. |
| JD-007 | judgment-day | WARNING | design.md § Testing Strategy (Core-alone load-bearing assertions row) | verified | Added a Core-alone load-bearing assertions row: "4-file rule", "400 changed lines", the 6 named trigger labels, and the 4 lens names must be assertable in core alone, plus disposition-mapped per-rule assertions replacing the blanket union-includes sweep. Not touched this round; the 4 lens names and 6 trigger labels are now confirmed present verbatim in the Appendix's Delegation Rules and 4R Review Triggers blocks. |
| JD-008 | judgment-day | WARNING (theoretical) | design.md § Decision: Three lazy files by domain / § Core Budget table (Language Boundary row) (vs assets/orchestrator.md § Language Boundary) | verified | LB3/LB4 (artifact/comment language) kept VERBATIM in core (not moved to lazy) — Inventory + Core Budget rows updated accordingly. Round 3: the Language Boundary core row is now a literal drafted block in the Appendix (1,393 B measured) — LB1 pointer verbatim + LB3/LB4 verbatim + a new LB2/LB5 pointer sentence — replacing the prior "+~1.1KB" estimate-only claim with actual measured text. |
| JD-009 | judgment-day | WARNING | design.md § Reserved: incoming Review Execution Contract (cross-design corroboration note) | verified | Added a corroboration note citing `port-review-ledger-contract/design.md` § Sequencing / Coordination (hard merge order AFTER persona-single-channel, BEFORE orchestrator-lazy-diet), closed by that change's own design content. Not touched this round. |
| JD-010 | judgment-day | SUGGESTION | design.md § Reserved: incoming Review Execution Contract / § Decision: Verbatim-to-lazy / § Testing Strategy (Regression row) | verified | Dropped the uncorroborated "gentle-ai's proven 7.8KB core" comparator; added the lazy-copy byte-identical/no-reflow constraint note to the Verbatim-to-lazy decision; fixed "gente-ai.test.ts" → "tests/gentle-ai.test.ts" in the Testing Strategy table. Round 3: the "original aspirational 7.8-7.9 KB" phrasing had been REINTRODUCED into the Core Budget Decision's Rationale paragraph after this row's round-1 fix (a comparator regression) — removed again; the threshold rationale now stands on the measured 22,626 B → 9,945 B numbers alone, with no aspirational/comparator figure anywhere in the Decision. |

Round 3 — scoped re-judge: judge B CLEAN (all 10 RESOLVED; +1B trailing-newline convention noted); judge A NOT CLEAN on JD-001/JD-004 via a working-tree baseline (24,047B) vs judge B and the design itself referencing HEAD (22,626B, correct — port-review-ledger-contract was uncommitted). Orchestrator arbitration: B formal frame correct, A actionable facts adopted via the design Addendum (Reserved row becomes a SPLIT row against the real 1,733B landed section; re-baseline commitment now targets the post-merge file; H3 heading noted; sequencing note reconciled in both designs — ledger-port landed first, functionally independent of persona; diet remains hard-LAST). Terminal state: JUDGMENT APPROVED (design), conditional re-baseline executes at apply per the Addendum.

## Post-apply round — surgical fix pass (sdd-verify CRITICAL + 2 judges, convergent finding)

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| PA-001 | sdd-verify + judgment-day (2 judges, convergent) | CRITICAL | assets/orchestrator.md § Review Execution Contract / tests/orchestrator-budget.test.ts | fixed | All three reviewers converged independently on the same finding: the core's `### Review Execution Contract` kept its heading + bold lead-in + all 3 persistence-branch bullets byte-identical to `orchestrator-delegation.md`'s copy (850 B raw), violating the design's "Verbatim-to-lazy, fresh-summary-in-core" decision, and the committed budget test could only pass because it measured against a short `mkdtemp` stub path rather than a realistic install path — at a realistic (≥59-char) install path such as `~/.pi/agent/npm/node_modules/gentle-pi/assets`, the substituted `getOrchestratorPrompt()` return measured 10,568 B, over the 10,240 B budget. Fixed by condensing the core block to 456 B raw (394 B cut) while keeping the branch names, the meaning of the empty-ledger rule, and the pointer to `{{GENTLE_PI_DELEGATION_PATH}}`; the full verbatim block stays only in `orchestrator-delegation.md`. Added a realistic-path-length assertion to `tests/orchestrator-budget.test.ts` (measured via a fresh child process, `tests/fixtures/measure-orchestrator-prompt.mjs`, because `ASSETS_DIR`/the prompt cache are module-import-time singletons) — RED at 10,568 B before the fix, GREEN at 10,174 B (66 B headroom) after. Real dev-checkout path (31 chars) also re-measured: 10,351 B → 9,957 B. |
| PA-002 | sdd-verify (orchestrator finding) | CRITICAL (packaging) | scripts/verify-package-files.mjs | fixed | `requiredPaths` did not include this change's 3 new lazy files (`assets/orchestrator-delegation.md`, `assets/orchestrator-memory.md`, `assets/orchestrator-skills.md`); a published npm package built without them would break `getOrchestratorPrompt()` at runtime. Added all 3; `node scripts/verify-package-files.mjs` now passes (44 files, was 41). |
| PA-003 | sdd-verify + judgment-day | WARNING (documentation honesty) | openspec/changes/orchestrator-lazy-diet/apply-progress.md | fixed | Corrected the empirically-false claim that "a shorter/typical install path will normally fall under 10,240 B" (real installed paths are LONGER than this dev checkout, not shorter); recorded the post-fix measured numbers (core raw `wc -c`, substituted return at the realistic path); corrected an implied "6 test-file repoints" framing to the accurate "5 repointed files + 1 new test file" (`tests/orchestrator-budget.test.ts` is net-new authorship, not a repoint); documented the REC condensation as the budget fix with byte evidence. |
| PA-004 | judgment-day (both judges) | SUGGESTION (accepted-latent) | tests/orchestrator-budget.test.ts § disposition-mapped union test | accepted-latent | The disposition-mapped union test's `.includes()` substring check is a union-membership weakness in principle (a line could theoretically match by accident against unrelated content in the wrong target file) — not tightened to an exact-line check in this pass. Both judges independently ran the equivalent exact-line check externally (outside the committed test) against the actual fixture/target files and confirmed 0 normative-line losses under the exact-line semantics too, so the latent weakness has zero observed impact today. Accepted as latent risk rather than fixed, to keep this pass scoped to the confirmed CRITICAL findings; a future pass MAY tighten `isNormativeLine`'s membership check to exact-line matching if the union test's blast radius becomes a concern. |
| PA-005 | judgment-day (judge A) | INFO (process) | n/a (judge A's local environment, not this repo's committed state) | resolved | Judge A hit an environment incident during this round (working-tree/tooling state issue in judge A's own review environment, not a repo-content defect). State was restored before this fix pass began; no repo files were left in a broken state. Rule adopted going forward: after any judge/reviewer environment incident, restore first and re-verify the affected surface with a fresh read before trusting that judge's findings for the affected area — applied here by cross-checking judge A's PA-001/PA-004 findings against judge B's and sdd-verify's independently before acting on them (all three converged on PA-001; PA-004 corroborated by both judges as described above). |

**Verification of this round's fixes**: `pnpm test` → 307/307 pass (306 baseline + 1 new realistic-path budget test), runtime harness green. `node scripts/verify-package-files.mjs` → passes, 44 files. Core raw: 9,713 B (`wc -c assets/orchestrator.md`, 121 lines). Substituted `getOrchestratorPrompt()` return at a realistic 68-char install path: 10,174 B (66 B headroom under the 10,240 B budget).
