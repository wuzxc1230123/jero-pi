# Review Ledger — review-contract-cleanup

Source: external cross-runtime review (user's Pi/OpenCode session, GPT-5.5, 2026-07-09)
of main HEAD~2..HEAD (#72, #74), run WITH the shipped review-ledger contract.
Findings verified by the orchestrator before acceptance.

| id | lens | location | severity | status | evidence |
|----|------|----------|----------|--------|----------|
| EXT-001 | reliability | assets/agents/review-{risk,readability,reliability,resilience}.md § Output contract | WARNING | fixed | "If clean, say exactly: `No findings.`" contradicts the ledger clause "persist an empty ledger record" in the same files. Root cause: the chain wording was fixed (JD-004) but the agents' own Output contract lines were never in any ledger. Fixed: replaced the Output contract sentence in all 4 review-*.md with wording aligned to the ledger contract ("return an empty findings ledger... never skip the ledger"); jd-judge-a/b.md and the chain file already carried consistent wording, no change needed there. |
| EXT-002 | reliability | scripts/verify-package-files.mjs § requiredPaths | WARNING | fixed | skills/_shared/review-ledger-contract.md missing — a published package could omit the canonical contract and the verifier would pass. Fixed: added the path to requiredPaths; verifier now reports 45 files. |
| EXT-003 | readability | assets/agents/review-*.md § Ledger persistence | WARNING | fixed | "read-only reviewer" + write-style persistence bullets read as agent write instructions; the execution-mode clause assigns persistence to the orchestrator but only at the end of the file. Fixed: added an orchestrator-ownership line immediately before the persistence block in all 4 review-*.md and jd-judge-a/b.md, outside the canonical hand-copied clauses so drift-guard assertions keep passing verbatim. |
| EXT-004 | resilience | skills/_shared/review-ledger-contract.md § Ledger persistence (canonical + all hand-copies) | WARNING | fixed | No degraded path if the engram upsert fails: the ledger is the recovery artifact; losing it silently breaks the fix/re-review loop. Gap also exists in gentle-ai's canonical (upstream follow-up). Fixed: extended the canonical `engram:` bullet with a fallback-and-report sentence and hand-copied it into all 9 adopting surfaces (4 review-*.md, jd-judge-a/b.md, judgment-day SKILL.md, prompts-and-formats.md Judge Prompt fence, orchestrator-delegation.md); core orchestrator.md only carries a summarized engram bullet (not verbatim), so it was left untouched per budget policy — confirmed via `tests/orchestrator-budget.test.ts` (36/36 green, unchanged). Clause added to `tests/review-ledger-contract.test.ts` ledgerPersistenceClauses first (RED: 10 failing), then assets updated (GREEN: 27/27). |
| EXT-005 | readability | tests/persona-single-channel.test.ts byte assertions | SUGGESTION | fixed | Byte literals (438/831/2117/817, deltas 379/682/-283/-341) repeated across assertions; named constants grouped by fixture would be clearer. Fixed: extracted a grouped, commented block of named byte constants and replaced the repeated literal occurrences with them; PRE_*/POST_* text fixtures and assertion outcomes unchanged (28/28 green). |
